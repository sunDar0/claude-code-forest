# agenTree — 기능 백로그 (기획)

다분야 기획 워크플로우(2026-06-23) 산출. 6개 분야 담당자(방치형 게임·데이터 스토리텔러·월드 생태·대시보드 UX·공유 내보내기·시스템 운영) 제안 → 누락 비평 → 회의적 타당성 평가 → 통합. 후보 40개(recommend 19·maybe 20·drop 1).

**이 문서의 위치** — 코드 현 상태는 `agenTree-spec.md`(역설계 spec), 기획 의도·히스토리는 `_workspace/00_architecture.md`. 이 문서는 **앞으로 만들 후보 모음(백로그)** 이다. 착수하면 해당 항목에 상태를 적고, 완료 시 git log·spec 로 흡수한다.

## 진행 상태 (2026-06-24 기준)

| 상태 | 항목 |
|------|------|
| 완료 | 이번 달 진행 게이지 + 숲 묶임 토스트 (DOM/HUD, 2026-06-23) |
| 완료 | 시간대 하늘 (로컬 시계 하루 색 사이클, 렌더, 2026-06-23) |
| 완료 | 셀 상세 토큰 약식 표기 (DOM, 2026-06-23, 유지) |
| 폐기 | 셀 상세 작업 성격 한 줄 (06-23 도입 → 06-24 평균 대비 침엽수 뱃지로 대체) |
| 완료 | 셀 상세 역대% 폐기 + 평균 대비 침엽수 삼각형 뱃지(1~3) (DOM, 2026-06-24) |
| 보류(이미 있음) | 오늘 단계 진행 예고 HUD — 호버 툴팁에 경험치 바가 이미 있음 |
| 대기 | 아래 티어 전부 |

## 하드 제약 (모든 후보 공통 — 평가 기준)

- **데이터·서버·집계·activation·gridPlacement·buildForest 반환 = 단일 소스.** 렌더+클라 순수함수 변경이 가장 안전.
- **§28 프리즈 0** — 매 프레임 픽셀루프 폭증 금지. 베이크·내용 시그니처·LRU 로 푼다.
- **골든 지문 가드**(`test/render-smoke.test.js`) — 외형 바꾸면 갱신·근거 기록.
- **결정성·팬 불변** — 월드 좌표 시드(화면좌표 시드 금지).
- **40캡 파티클·16:9 레터박스·FOREST_DEBUG 게이트·Node 핫리로드 없음**.
- 검증: 헤드리스 mock≠실모드, 실 data 불가침(/tmp 격리), 백그라운드 rAF throttle.

---

## 테마 1 — 진행감·보상 (방치형 진행 축)

공통 신호원은 이미 단조 증가로 추적되는 `dailyMaxTokens`·`refMax`. 가장 안전한 형태는 클라 순수 파생(HUD 숫자·스트릭), 가장 무거운 형태는 서버 finalize 에 카운터/플래그 영속. **신기록 신호(newRecord)를 한 번 만들면** HUD 레벨·축포·신기록 마커·연간 카드가 모두 공유 → 재사용 시너지 큼. 서버 침범은 최후, 클라 파생이 1차.

- **수목 레벨(신기록 승급) HUD 숫자 1차** [M] — `forest.days` 를 date 오름차순 훑어 `totalTokens` 누적최대 갱신한 날 수를 클라 순수 파생으로 셈 → "Forest Lv.N". 이 순수함수가 반환하는 **신기록 날짜 Set** 이 축포·마커·연간카드의 공유 신호원.
- **이번 달 진행 게이지 + 숲 묶임 토스트** [S] — `forests[ym].monthly.activeDays / 그 달 일수` 막대 + 자동 묶임 false→true 순간 완성 토스트. → **완료(2026-06-23)**. ui.js updateHud 의 기존 activeDays·daysInMonth 재사용(새 집계 0). 막대=#hud-tr-body 자손(접힘 자동), 토스트=detectAutoBundle false→true 분기 1줄(lastBundled 가드 1회성). 골든 불변(36/36). 상세 `_workspace/20_forest_game_report.md`.
- **신기록 갱신 순간 축포** [M] — 오늘 `dailyMaxTokens` 새로 넘긴 순간 기존 금가루 풀(40캡) 안에서 오늘 나무 발생률·밝기 일시 상향 후 원복. 새 파티클 시스템 0.
- **연속 사용일 스트릭 + 끊김 표식** [M] — 며칠 연속 나무 심었나.
- **신기록 레벨 — 서버 파생 영속(충실판)** [M~L] — `forest.json` 메타에 `userLevel`·`gridRecordCount` 영속. "하루 1표" 결정성 위해 finalize/동결 경로에 dedup. 부팅 시 과거 finalized 재생으로 소급 재구성.
- **그리드 레벨 = 영속 화관 / 신기록 황금 깃발** [M~L] — 신기록 깬 셀에 영속 표식(황금 잎 화관·별·황금 깃발). day 메타에 `recordBreak` 불리언 박고 클라가 표식만 렌더. 붉은 깃발과 색·형태 분리.

## 테마 2 — 살아있는 세계 (앰비언트·계절·하루)

전부 라이브 패스 또는 정적 베이크 변경이라 데이터·서버 무관. 시간대 하늘이 가장 합의도 높고(`palette.js` 시각 보간 헬퍼) 야생동물·날씨·계절이 그 토대를 재사용. **안개 계열은 과거 폐기 이력**(`decision-agentree-fog-of-war-abandoned`) 있어 사용자 합의·라이브 캡처 게이트 선행 조건.

- **시간대 하늘 (로컬 시계 하루 색 사이클)** [S] — `_drawSky` 의 skyTop/skyBot·구름·헤이즈를 로컬 시각으로 보간(새벽 보라→한낮 파랑→황혼 주황→야간 남색). 골든은 시각 정오 고정으로 결정성 유지. → **완료(2026-06-23)**. `palette.js` 에 순수 헬퍼 `skyColorsAt(hour)` + 키프레임 5개(0/6/12/18/22시, wrap 으로 24=0 닫힘). `render()` 진입에서 시각 1회 읽어 `this._skyHour` 주입(`_readClockHour` 1곳에만 `new Date()` 격리, 테스트/스냅샷은 `setSkyHour` 고정 주입). `_drawSky`·`_renderOverview` 둘 다 주입값으로 `skyColorsAt` 호출. 하늘은 라이브(스냅샷 분리)라 시각이 베이크 키에 안 들어가 §28 재베이크 0(헤드리스로 0~23시 스윕 → groundBake 키 변경 0·스냅샷 무효화 0 측정). 정오 키프레임 = 기존 `PAL.skyTop/skyBot` 동일이라 골든 `e90c05af` 불변. 단일 그라데이션 유지(2단 밴드 금지, `fog-of-war-abandoned` 함정). 야간 남색은 검정 아님(rgb 합 100+)으로 금가루·깃발·나무 대비 유지. 실모드(5199 자체 서버) 캡처로 정오 파랑·야간 남색·황혼 주황 확인. 새 테스트 `test/sky-time.test.js` 6케이스. 상세 `_workspace/20_forest_game_report.md`.
- **야생동물 앰비언트 (새+토끼)** [M] — 나비 1종을 새(가로 횡단)·토끼(풀숲 폴짝)로 확장. 등장 빈도는 기존 density(requestCount) 재사용. 월드 시드.
- **계절 색조 — 월별 숲 톤** [M] — 묶인 숲 군집 바닥/풀을 ym 결정값으로 봄 연두/여름 짙은초록/가을 황갈/겨울 차분 시프트. §8-2 "한 해 리듬을 12개 다른 숲으로" 미구현분. `_groundBakeKey` 에 월톤 시그니처 추가.
- **날씨 앰비언트 (햇살 1차, 안개 게이트)** [M] — 성목=햇살 광선 가닥, 저활동=옅은 안개. 햇살은 저위험, 안개는 폐기 이력 게이트.
- **가벼운 날씨 입자 (비·눈, 자체 24캡)** [M] — 40캡 금가루와 별개 24캡 레이어. 날짜 시드 결정성. off 토글이 채택 조건.
- **숲 지형 고저차 미세 음영** [L] — `_fbm` 재사용 미세 둔덕/골 음영(밝기 변조만). 음영 입력은 동결 과거 군집 density 로 제한.

## 테마 3 — 사용량 인사이트 (읽기·이해)

전부 DOM 텍스트/툴팁 변경이라 골든·§28 무관·즉효. `pickDistribution` 등 기존 분류 재사용으로 effort 작음. 범례는 다른 모든 시각 후보의 가치를 끌어올리는 메타 기능.

- **셀 상세 '작업 성격' 한 줄** [S] (2026-06-23 도입 → **2026-06-24 폐기**: 역대% 와 함께 자연어 멘트 제거 → 평균 대비 침엽수 뱃지로 대체. 코드에 `distributionLabel` 호출 0) — 5메트릭 raw+% 에 **비율 기반** 자연어 캐릭터 한 줄. **절대량 금지**(cacheRead 가 항상 압도적이라 매일 "캐시 많이 읽은 날"이 됨) → `pickDistribution` 의 그날 안 비율(output/input·cacheRead/input 치우침)로 분류. "~경향"·"~위주" 보수 문구. → **완료(2026-06-23)**. `metrics.js` `pickDistribution` export(로직·반환값 불변) + 새 순수함수 `distributionLabel(distribution)`(4분류→보수 문구, 모르는 값 ""). 단일 소스 = `cellInfo` 가 `params.sim.distribution`(이미 pickDistribution 결과, 재계산 0) + `totalTokens`(토큰 4종 합) 노출. ui.js updateDetail 사용량 섹션 직후 `.drow "작업 성격"` 한 줄, `totalTokens>0` 일 때만(EMPTY 는 기존 empty 분기라 자동 미표시). **비율 기반 증명**: cacheRead 1.2억(절대 압도)이라도 input 2천만이면 readRatio 6≤12 → weeping 아닌 uniform(헤드리스). 골든 `e90c05af` 불변(DOM 만). 한계: 실데이터·mock 둘 다 cacheRead/input>12 라 라이브에선 weeping 지배(정직한 분류) → 4종 다양성은 헤드리스 단위테스트로 증명. 상세 `_workspace/20_forest_game_report.md`.
- **숫자 가독성 — 토큰 약식 표기** [S] — 툴팁 raw(478,231,905)를 기존 한글 `abbr()` 로 약식. 상세 모달 raw 유지. → **완료(2026-06-23)**(작업 성격과 묶음). `cellInfo` 가 raw 원시 숫자 노출 → ui.js updateTooltip 이 `abbr` 로 5메트릭 약식(`33.7만`·`2.6억`, 서구식 M/K 아님). 상세 모달 rows 는 `toLocaleString` 정밀 콤마 유지(`336,851`) — 툴팁·상세 분리. 라이브 확인(5188 자체 서버 mock/실): 툴팁 약식·상세 정밀 둘 다 동시 동작·콘솔 0.
- **호버 미니 추세 스파크라인** [S] — 호버 툴팁에 직전 7일 totalTokens 도트 스파크라인. `cellList` 가 이미 date 오름차순 전체 보유 → 슬라이스만. 분모(refMax vs 7일 로컬최대) 결정 필요.
- **이 숲이 왜 이렇게 생겼나 — 시각 인과 범례** [M] — 픽셀↔메트릭 매핑(버섯=requestCount·금가루=cacheRead·단계=일일토큰·풀=그늘거리·붉은깃발=마지막활성) 토글 카드. 문구 근거=`agenTree-spec.md §4` 표(죽은 sim 매핑·sap.density 제외). 도트 샘플 오프스크린 1회 베이크.
- **연간 회고 카드 (올해 숲 요약)** [M] — `yearly[year]` + days 파생(성목수·신기록 일수·최장 스트릭·가장 센 날) 1장 DOM 카드. 클릭 시 1회 계산. 레벨/스트릭 순수함수 재사용.

## 테마 4 — 공유·내보내기 (밖으로 자랑)

기존 베이크 산출물(맵 스냅샷 캔버스)을 파일로 빼내는 군. **숲 스냅샷 PNG 가 토대**고 카드·타임랩스가 그 위에 쌓임 — 한 회차로 묶어 캔버스 추출 토대를 두 번 안 만드는 게 핵심. 외부 중앙 서버 종속(업로드 동봉)은 검증 불가라 분리.

- **숲 스냅샷 PNG 내보내기** [S] — 이미 굽힌 맵 스냅샷 캔버스를 `toBlob('image/png')`로 파일화. 새 렌더 패스 0. **A안(뷰포트 해상도·즉시) vs B안(풀해상·클릭당 1회 베이크) 결정 필요**.
- **공유용 연간 요약 카드 (PNG)** [M] — 스냅샷 위에 시작일·총 나무·성목·총 토큰·역대최대일 통계 합성→PNG. 선행: 숲 스냅샷 PNG.
- **숲 성장 타임랩스 리플레이 (수동 트리거)** [L] — startDate→오늘을 압축 재생(날짜순 페이드인+숲 묶임). 데이터 이미 클라에 다 있음. 진입/복귀 상태 격리가 주의점.
- **성장 타임랩스 PNG 시퀀스 내보내기** [L] — N(≤60)프레임 각 시점 "그날까지 숲" 오프스크린 베이크→캡처. GIF 는 외부 의존성이라 연속 PNG 1차.
- **정적 숲 공유 페이지 export (읽기 전용 HTML/JSON)** [M~L] — `GET /api/forest` 응답을 파일로 떠 서버 없이 열람만. `file://` CORS 주의.
- **공유 카드에 레벨 배지** [—] — 레벨 후보 보강.
- **업로드 설정에 PNG 스냅샷 동봉** [—] — 외부 서버 수신 미공개라 보류.

## 테마 5 — 운영·접근성 (오래 켜두는 신뢰)

종일 켜두는 도구의 신뢰·건강성. 폴 신선도·미심기 알림·온보딩·모션 줄이기는 데이터 무관·저위험. 진단 엔드포인트·백업 CLI 는 서버 읽기 전용.

- **오늘 미심기 알림 (늦은 밤 배지)** [S] — 오늘 active=false 이고 로컬 22시 이후면 HUD 은은한 배지. 클라 순수 파생.
- **데이터 무결성·진행 진단 엔드포인트 (GET /api/health)** [S] — `_debugSnapshot` 화이트리스트 페이로드 + 일관성 체크. 서버 읽기 전용 1라우트.
- **폴 신선도 표시 + 백그라운드 폴 완화** [M] — `generatedAt` 으로 "N초 전 갱신" + 백그라운드 탭 5초→30초. `visibilitychange`.
- **첫 실행 온보딩·빈 숲 안내** [S] — 콜드(활성0)일 때 중앙 안내 + 첫 클릭 셀 하이라이트, 첫 나무 심으면 소멸.
- **접근성 — 모션 줄이기 토글** [M] — `prefers-reduced-motion` 존중 + 설정 토글. 라이브 패스 모션만 게이트(정적 바닥 유지).
- **콜드 리셋·백업 운영 CLI 서브커맨드** [S] — `store.js` 에 `--backup`(data/ 타임스탬프 복사)·`--snapshot`(요약). `boot()` 미호출이라 서버 메모리 무변경.
- **잘못 심음 되돌리기 (오늘 셀 한정)** [L] — 오늘 셀(미동결·active·당일)에 한해 되돌리기. 서버 파괴적 변이 신규라 게이트 경계 검증 필수(거부 경로 8종).

---

## 결정 대기 (openQuestions)

착수 전 사용자가 정해야 할 것:

1. **신기록 판정 기준** — (A) `dailyMaxTokens` 단조 최대(단순·초반 쏠림) vs (B) 최근 N일/p95(자주 깨짐·"훈장" 의미 충돌) vs (C) 셀 stage 신기록(데이터 계약 큼). 레벨·화관·축포가 다 여기 걸림.
2. **레벨 의미·보상** — 표시만(자랑거리)? 식생·연출 보상까지? → 클라 파생(안전) vs 서버 영속(충실) 갈림.
3. **스트릭 경계 규칙** — 오늘 active 면 포함? 미사용이면 끊김 vs "진행중".
4. **숲 스냅샷 PNG** — A안(뷰포트·즉시) vs B안(풀해상·1회 베이크). 카드·타임랩스가 재사용하므로 먼저.
5. **스파크라인/연간 카드 분모** — refMax 일관(§62) vs 7일/연 로컬최대(가독).
6. **안개 계열** — 과거 폐기 이력. 재도입 시 라이브 캡처 게이트(§28 fillRect 증분+시각 판정) 전제.
7. **3번째 이후 앰비언트 off 토글** — 노이즈화 방지가 채택 조건. 모션 줄이기와 통합?
8. **되돌리기 노출** — 활성화 직후 토스트만 vs 상시 버튼. 게이트 경계 검증 범위.
9. **PNG 업로드 동봉** — 외부 서버 수신 미공개. 보류 vs 외부 계약 먼저 확인.
10. **기존 'Forest Level(treeDays/5)' 표기** — 코드에 있다면 새 신기록 레벨과 의미 충돌 정리 필요.
