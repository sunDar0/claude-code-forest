# agenTree — Claude Code Forest

일간 Claude Code 사용량을 절차적 도트 그래픽 숲으로 보여주는 로컬 폴링 방치형 대시보드.

## 하네스: Claude Forest

**목표:** `~/.claude/projects/**` 의 사용량 jsonl 을 일자별로 집계해, 하루 = 나무 한 그루로 자라는 도트 숲으로 시각화한다.

**트리거:** Claude Forest / 사용량 숲 / 사용량 게임 / 나무 대시보드 / agenTree 관련 작업(생성·수정·재실행) 요청 시 `claude-forest-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**단일 진실 소스:** `_workspace/00_architecture.md` (데이터 계약 §1, 메트릭 매핑 §2, 게임 모델 §3, 폴링 §4, 렌더 규칙 §5). 데이터 집계 기준 구현은 `~/work/claudeCode-thirdParty/claude-usage-tracker`, 렌더 이식 출처는 `~/study/labs/tree-growth-simulation`.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-08 | 초기 구성 | 전체 (agents 3, skills 4, 아키텍처 계약서) | 명세서 기반 하네스 신규 구축 |
| 2026-06-08 | 구현 + QA 통과(8/8) | server/, public/ | 데이터레이어·캔버스 클라이언트 구현, 경계면 교차검증 |
| 2026-06-08 | 캔버스 풀스크린 버그픽스 | public/js/renderer.js | 육안 검증서 캔버스가 ~300px 로만 렌더되던 결함 수정(뷰포트 기준 정수배 스케일) |
| 2026-06-08 | 렌더 방향 전면 전환 | 계약서 §5, public/js/{renderer,simulation,grid,particles} | 사용자 피드백·키 레퍼런스 이미지 반영: 아이소메트릭·labs 이식 폐기 → 스타듀밸리식 정사각 타일 탑다운 + 절차적 활엽수 + 골격 경로 수액 + 밝은 팔레트 + HUD 패널. 데이터레이어·서버 불변 |
| 2026-06-08 | 나무 아트 패스 ×2 | public/js/{simulation,renderer} | 기둥 굵게·수액 가시화·수관 입체 / 수관 달걀→둥근 크라운·바닥 그리드→잔디 타일 텍스처 |
| 2026-06-08 | 카메라 줌/팬 + 그리드 모델 정정 | 계약서 §3·§7, public/js/{renderer,main,grid,simulation} | 휠 줌(그리드 한 칸~활성 전체)·드래그 팬 / '하루=1타일'→'하루=1그리드(9타일)' 구조 바로잡음 |
| 2026-06-08 | 기획 고도화: 계층 구조 | 계약서 §8 | 타일/그리드/숲(월)/연간 3계층 + 월간 수동 숲 묶기 + 숲 형태 가변. 다음 페이즈 설계(미구현) |
| 2026-06-08~09 | §9~§17 누적 진화 | 계약서 §9-17, public/js/*, server/ | 렌더 디테일·툴팁/상세·방향성 배치(시드)·DOM UI 전환·XP/단계(5시간 cap 역산, 20/30/50%)·수액폐기→성목 파티클·오늘부터 시작+그리드 선택 활성화(선택→확인→빛기둥) |
| 2026-06-09 | 카메라 점검 | 계약서 §7-5, public/js/renderer.js | 줌 피벗=오늘 그리드·전방향 팬·해상도 정합·하늘 그라데이션 |
| 2026-06-09 | wiki 증류 | my-wiki | topics/claude-code-forest 허브 + concepts 3 등록 |
| 2026-06-09 | §18 데이터 영속+계층 계약 | 계약서 §18 | 사용자 제약 13개 확정: localStorage·무상태 → `data/` 서버 영속, 일/월/년 파일 분리 + 인메모리, 상태 일자별 저장(형태=시드 a방식), finalized 동결, 활성화=자리+그날 데이터. 구현 대기 |
| 2026-06-09 | §18~§20 구현 + QA 전항목 통과 | server/{store,mock,index,aggregate}, public/js/*, data/, mock/ | data 영속(인메모리+파일)·일/월/년 3 API·jsonl→data 갱신·finalized 동결 / mock 목업 427일(2025-04~2026-06) `?mock=1` 서빙 / §18-11 콜드스타트 첫 그리드 자유선택(startDate부터) / §19 성목 평컷·밑동수평 제거 / §20 카메라 활성중심 폐기(자유팬)·줌아웃 배치범위+백버퍼상한·전장의안개·커서프리뷰·하늘확대·HUD접기. 검증은 /tmp 격리(실 data 불가침) |
| 2026-06-09 | §21 시작일 모달 + §20 정정 | 계약서 §21·§20-2·§20-3, server/{store,index}, public/js/*, data/ | 시작일 모달(startDate null→사용자 입력→영속, 미래/형식/중복 거부)·시작일~오늘 그리드 활성화 / 줌아웃 콘텐츠+여백(답답 해소)·맵 풀밭 한 톤(detail LOD 폐기)·안개/가장자리 페이드 **전면 제거**(사용자 요청). data 콜드 스타트 복원 |
| 2026-06-09 | §22 + 세션 인수인계 | 계약서 §22, public/js/{renderer,main}, `_workspace/23_session_handoff.md` | 모든 칸 좌표·외곽선 1px·비활성 그리드 진한흙+잡초 / 컨텍스트 비대로 새 세션 인계. **미해결**(줌아웃 최대·카메라 센터·외곽선 정렬·좌표 실모드 미표시)·교훈(헤드리스 mock≠실모드, 실측 필수)은 23_session_handoff.md 참조 |
| 2026-06-10 | §24 맵 경계 클램프 + 오버뷰 줌 | 계약서 §24, public/js/renderer.js, `_workspace/24_map_bounds_overview.md` | 맵 밖(0,0 미만·99,39 초과) 타일/라벨 절차 생성 제거·팬 클램프 맵 bbox 정확 일치 / 줌아웃 끝 = 오버뷰(화면 너비에 맵 5400px 맞춤 분수 배율, 맵 하단 고정+위 하늘, 정적 스냅샷 캐시로 freeze 없음). 실모드 픽셀 실측 통과(미해결 #1 해소) / 후속: 지평선=맵 북단 일치(하늘~땅 미채색 띠·드래그 잔상 제거), 오버뷰 풀 디테일 베이크(맵 1:1 1회 그리기→축소, 구름 흐름, 진입 1회 134~251ms 멈칫은 수용 확정) |
| 2026-06-10 | §25 나무 뎁스 레이어링 + 숲 군집 | 계약서 §25·§8-2(블롭 폐기 표기), public/js/{simulation,renderer}, `_workspace/20_forest_game_report.md` | 성목 몸통 수평절단 재발 근본수정(기둥 강한 곡선 테이퍼+좌우 비대칭폭 wL/wR·컷 선형보간 클립·불규칙 뿌리 플레어로 상·하단 평컷 제거) / 숲 블롭("초록 애벌레") 폐기 → 개별 작은 나무 수십그루 군집(buildForest ym시드 결정적 배치·forestTrees ym캐시·Y-sort·extent 정합). 경계면 불변(trunkSegs 옵셔널 wL/wR만 추가). 헤드리스 스모크 통과(런타임에러0·결정성·군집33그루 베이크), 실모드 픽셀 실측은 오케스트레이터 후속 |
| 2026-06-10 | §26 §25 버그 3건 수정 | 계약서 §26·§25-2·§8-2(정정 표기), public/js/{simulation,renderer}, `_workspace/{00_architecture,20_forest_game_report}.md` | 버그1: 숲 군집 타원 배치 폐기→멤버 셀 좌표 기반 배치(buildForest cells+origin·월드px 절대좌표·셀당 2~5그루·전역 Y-sort·_drawForestFloor·bbox extent 정합) 펼침 전후 위치 일치 / 버그2: 화면좌표(sx) 시드 폐기→월드 불변 시드(_seedNum·seedVal 전달·노이즈 위상 시드화) 팬 요동 제거 / 버그3: 블롭별 구체 음영 폐기→수관 통합 음영(합집합 스캔라인 마스크·전체 기준 빛방향 램프·결정적 질감점·뒤/앞잎 레이어). 데이터·서버 불변. 헤드리스 스모크 전항목 통과(오프셋후 픽셀동일 mismatch0·결정성·군집100그루 베이크), 실모드 실측은 오케스트레이터 후속 |
| 2026-06-10 | §27 스프라이트 베이크(perf) + 클럼프 음영 + 뿌리 줄무늬 | 계약서 §27(§26-2·§26-3 정정 표기), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 문제1(최우선·버벅임): 매 프레임×나무마다 즉석 마스크/픽셀루프 → **나무 스프라이트 베이크**(한 그루 1회 오프스크린→drawImage 1콜, _paintTreeBody/_treeBBox/_treeSprite 분리, 키 date\|톤\|stage·f\|seed, setData/군집 재베이크시 무효화, LRU512, 픽셀퍼펙트 정수 drawImage·스케일변환 없음). 측정: 나무 본체 프레임당 fillRect 18그루 ~20.8k→0·100그루 ~196k→0(베이크 1회로 이동) / 문제2(판떼기): 합집합 실루엣 유지+마스크 내부 **클럼프 경계 음영** 복원(블롭 rim 기준 초승달 그늘+캡, 구체 중심 그라데이션 아님) / 문제3: 뿌리 세로기둥+dx&1 폐기→**가로 스캔라인**(행단위 색교차)+사선 테이퍼 뿌리. 데이터·서버 불변. 헤드리스 스모크 전항목 통과(캐시적중 rebake0·결정성·LRU상한·스테이지무효화), 실모드 FPS·육안은 오케스트레이터 후속 |
| 2026-06-10 | §28 프리즈 근본수정(군집=달 1장 베이크) + 베이크 예산 + 레퍼런스 나무 아트 | 계약서 §28(§27-1 정정 표기), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | **실모드 완전 프리즈 실증·수정**: §27 그루당 스프라이트가 목업(427일·14묶인달) **~2,130장 > LRU512** → 매 프레임 전량 축출·재베이크 스래시(헤드리스 재현 프레임당 **2,131 베이크·~4.08M fillRect**, 실측 첫 rAF 8.9초·45초+ 무응답). 수정: ①군집=**달 1장 베이크**(_forestSprite/_drawForest, 스프라이트수=달수~14, 내부 Y-sort 닫힘, 전역 Y-sort 1객체) _drawForestTree·f\|seed 폐기 ②**작업집합<상한**(일반 나무·군집 화면밖 컬링·상한 512→256) ③**프레임당 베이크 예산 3**(콜드/드릴다운/줌 분산, 오버뷰만 해제) ④같은키 재베이크0 점검. 아트: 티어 수관(rim 아랫면 차가운 그늘·윗면 밝은 캡)·**색상시프트 그늘**(leafCool 청록→leafCoolDeep 남보라, PAL/PAL_PAST 3색 추가)·차가운 외곽선. 데이터·서버 불변. 스모크: 프레임당 베이크 2,131→0·fillRect ~4.08M→~6.7k·결정성·런타임에러0 / 한그루 픽셀: 상단휘도138.7>하단109.0·차가운그늘552px·외곽선66px. 실모드 FPS·육안은 오케스트레이터 후속 |
| 2026-06-10 | §29 비차단 렌더(점진 베이크 + 내용변경시만 무효화) | 계약서 §29(§24-3·§28-4 보정), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | **§28 후에도 실모드 주기적 freeze 실증·근본수정**(로드 ~50초→잠깐 alive(좌측 숲 플레이스홀더)→3초뒤 45초+, 입력무관). 원인(헤드리스 fillRect/블록타임): **Path A** 오버뷰 스냅샷이 풀맵 5400×2160+전군집 `Infinity` 예산 **한 동기호출**(527k fillRect/실모드 수십초) + `setData` 가 **폴마다 무조건** _mapSnapshot=null → 오버뷰 중 매 폴 재베이크 / Path A부: 군집 달1장 한 호출 180그루(frame0 1.42M fillRect/328ms) / Path C: 동기블록 부산물 / **Path B(mock 비결정) 기각**(이미 결정적). 수정: ①**군집 점진 베이크**(_instBakeBudget 10그루/프레임 누적·부분반환·done시 0작업, _frameBakeBudget 3→2) ②**오버뷰 스냅샷 2단계 점진**(_advanceMapSnapshot: 워밍업 프레임들+합성 단1프레임, 그동안 저해상 임시본, 합성 LOD로 결텍스처 생략) _bakeMapSnapshot 폐기 ③**내용 시그니처**(_contentSignature+setData 조기반환, 무효화=실변경·resize 만). 렌더만, 데이터·서버 불변. 측정: 로드 최대블록 328→59.6ms·fillRect/frame 1.42M→131k / 같은내용 폴×5 → 베이크0·무효화0 / stage변경 1셀 → 1베이크(그셀만) / 오버뷰 같은내용 폴 → 스냅샷유지 0.2ms / 콜드오버뷰 워밍55프레임(각8~15ms)+합성18.6ms·단일블록>100ms 0회 / 결정성·런타임에러0. 실모드 라이브 freeze 해소는 오케스트레이터 후속 |
| 2026-06-10 | §30 스프라이트 에셋 교체 + 숲 경계 유기화 | 계약서 §30(§25·§28 절차아트 폐기표기), public/js/{sprites(신설),renderer,simulation}, server/index.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 절차 나무 아트("디자인 구리다") → **사용자 제작 스프라이트 시트**(tree_sprite_packed.png 716×1056·coords 5수종×5단계) 교체. 비동기 로더(sprites.js: speciesFor 가중[24,24,14,24,14]·frameNameForStage·onReady) / 단계매핑 SAPLING→s0·YOUNG→s1·MATURE stageProgress 3분할 s2~4 / 수종 날짜시드 결정적(단풍·민트 낮게) / 스케일 _niceScale 단순분수(성목~1/4·묘목~1/3) 베이크시 1회 nearest·밑동 하단중앙 앵커 / PAST 톤 source-atop 멀티플라이(알파보존) / 베이크 인프라(셀1장·군집달1장·예산·컬링·§29 점진)·파티클·툴팁·활성화 불변, 페인트소스만 시트blit(폴백=절차) / **숲 1자절단 해소**: buildForest 경계셀 오버행(빈이웃 개별방향 ~1타일)·그루수±2·스케일변주·_drawForestFloor 각도별 시드노이즈 침식·bbox 오버행 정합 / server MIME .png 1줄(정적서빙, 데이터계약 무관). 검증: node --check 전파일·25프레임 매핑·수종분포 일치·로드전후 warm 0베이크·로드실패 폴백·substage변경 그셀만1베이크·buildForest 결정성·런타임에러0. 실모드 육안·FPS 오케스트레이터 후속 |
| 2026-06-10 | §31 §30 보정 3건(경계 전방향오버행·밀도완화·숲바닥=잔디) | 계약서 §31(§30-4·§30-5·§26 보정), public/js/{simulation,renderer}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 스크린샷 3건. 렌더만, 데이터·서버 불변. ①**경계 직선 잔존**(§30-4 1방향 오버행→변·모서리 직선): buildForest 빈이웃 **8방향**(대각포함)·경계셀 그루를 **모든 빈방향 독립** 오버행(누적·상쇄없음·대각√2정규화)·셀좌표 cellPhase/cellStrength **탈상관**(같은변 이웃정렬 깸)·overhang half×1.2 ②**너무 빽빽**(§26 셀당2~5→): perCell=clamp(round(1+density×2),1,3) **1~3**·경계±1도 clamp·**최소간격 리젝션**(시드 6회시도 minGap=half×0.55, 못찾으면 마지막→최소1그루)·"많은달=빽빽"을 그루수→큰단계 비중(matureP density×0.45) ③**바닥 흙겉돎**(어두운음영덩어리→): _drawForestFloor 불투명 **초록풀밭**(PAL_PAST.grassA=#4f9436 활성서 PAST보정)+활성같은 풀잎결(셀당8~13, LOD선 생략)·외곽 내부셀사각/경계만 침식(직선금지)·8방향1.28배 확장·세로반경0.5(흙띠제거). 검증: node --check·buildForest 결정성·origin평행이동·셀당1~3(전density)·최소간격98%·**경계직선성 0건(60변)**·extent오버행확장·_drawForestFloor 런타임0/결정성/LOD결생략. 실모드 육안·FPS 오케스트레이터 후속 |
| 2026-06-10 | §32 §31 재보정(바닥=셀합집합 연속채움·밀도1~2) | 계약서 §32(§31-3·§31-2 보정), public/js/{renderer,simulation}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 실모드(`?mock=1`) 픽셀실측 미해소 3건. 렌더만, 데이터·서버 불변. ①**(최우선)바닥 가로띠 끊김**: §31-3 셀별 타원패치(세로falloff t=1-ty²·ery=GRID×0.5·경계 √t) → 인접셀이 위아래 가늘어져 **행사이 코너 빔**(가로 초록띠+흙 간극) 실증. **셀 footprint(GRID×GRID) 불투명 사각 풀채움**(fillRect cx-half,cy-half,GRID,GRID — 정수·동일폭 변공유 **내부이음매0**)+**빈이웃 향한 변에서만** 셀·방향시드 ±2px 침식/확장(평균+0.4), 내부변 불변 ②**밀도 1~2**: perCell=clamp(round(1+density),1,3) 대부분1~2·경계±1 상한 maxPer=density≥0.9?3:2(**3은 최상위만**)·minGap half×0.55→**0.75**·TRIES 6→12 ③경계직선 §31-1 오버행 유지·바닥외곽 탈상관 재확인. 검증: node --check·**바닥연속성(신규) 내부footprint 8748px 간극0/hole0**·minGap0.75 충족률 **전density 100%**·외곽직선성 최장평탄런 65px<3셀162px·결정성·런타임0. **실모드 실측 후속(오케스트레이터 직접 보정)**: ±2px 확장노이즈가 일반 줌에서 식별불가 → 외곽 변 **E(≈6px) 인셋 + 2px스텝 저주파 침식/확장 스캔라인**(len=E±E)으로 교체(§32-1 보정 표기). 실측: 바닥 연속·경계 굽이침·밀도 여유·콘솔에러0 확인 |
| 2026-06-10 | §33 숲 가장자리 나무 세로직선 잘림 수정(군집 베이크 bounds=그루 그릴 사각 합집합) | 계약서 §33(§28 정정 표기), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 스크린샷 2건: 묶인 달 가장자리 나무 반쪽만 그려짐(수관 세로직선 절단, §31 오버행 나무). **실증 원인**: §28 `_forestSprite` 캔버스=`cluster.bbox`(그루 footprint ±half*0.5≈13.5px)+단일 up/down → 실제 그릴 사각(시트 성목 폭 dw/2≈25px·§31 오버행 ~1.2타일)을 못 덮어 가장 바깥 밑동 프레임이 캔버스 밖 nearest 클리핑(회귀 실측: 옛 bounds **3/18그루·14.5px** 컷). 즉 buildForest footprint bbox 식 ≠ 베이크 그릴 사각 식(좌우 프레임폭·오버행폭 누락). 수정: `_instDrawRect`(시트=_blitSheetTreeInto·절차=_treeBBox 식 정합)→`_computeClusterDrawBounds`= ⋃그루 그릴 사각 ∪ 바닥 합집합(floorPad=E·풀잎결 reach) ∪ 안전마진4 → `drawBBox/drawUp/drawDown`(footprint bbox 와 별개). `_forestSprite` 캔버스·앵커(rx=wx0-x0,ry=wy0-y0)·`_drawForest` blit·`_contentWorldBounds`(§24 extent)·§28 컬링 모두 drawBBox 정합(나무 월드위치 불변·잘림만 제거). 데이터·서버 불변. **클리핑 단위검증(신규, 스텁캔버스 paint추적)**: 시트/절차 18그루 (A)drawBBox밖 0/18·worst0px (B)캔버스밖 paint 0 (C)가장자리1px접촉 0·결정성·node --check. 실모드 육안·FPS 오케스트레이터 후속 |
| 2026-06-10 | §34 오버뷰 스냅샷 이중 버퍼(드릴다운 초록 플래시 제거) | 계약서 §34(§29 보정), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 실측: 최대 줌아웃(오버뷰)서 숲 클릭 시 초록 화면 1~수 프레임 번쩍. **실증 원인**(추정 적중): 숲클릭=setDrilldown→setData→내용시그니처 변경→`_invalidateSnapshot()` 가 `_mapSnapshot=null`(카메라 오버뷰 유지)→다음 프레임 `_advanceMapSnapshot` 미합성→`_snapProvisional`(풀밭 한톤=초록) 노출=플래시. 근본: 스냅샷 교체에 **직전 완성본 유지하는 이중 버퍼 부재**. 수정: `_mapSnapshotStale` 신설·`_invalidateSnapshot`=완성본 유지+stale 표시(빌더/임시본만 리셋, 콜드만 null)·`_ensureMapSnapshot`=stale 이면 전진하되 stale 완성본 표시·`_advanceMapSnapshot` 미완성 분기=**stale 완성본 우선**(임시본은 콜드 전용)·완성 시 원자 교체(stale=false). 줌인 전환 피벗(zoomAt)·일반 줌 렌더는 스냅샷 무관 정상. 렌더만, 데이터·서버 불변. **헤드리스 프레임-소스 로깅**: 드릴다운 재합성 4프레임 표시소스 전부 stale-complete·**provisional(초록플래시) 0**·frame4 원자 교체 / 콜드 첫프레임 provisional 유지(요구3) / 같은내용 폴 무효화0 / 결정성·런타임0·node --check. 실모드 라이브 플래시0·FPS 오케스트레이터 후속 |
| 2026-06-10 | §35 좌표 라벨 디버그 모드화 | 계약서 §35(§22 보정), public/js/{renderer,main}.js | 렌더 안정화 확인 후 그리드 좌표 라벨 기본 OFF → `?debug=1` 에서만 표시(renderer.debug 가드, 오케스트레이터 직접 적용). node --check·실모드 실측(기본 라벨 없음·debug=1 표시) |
| 2026-06-10 | §36 숲 생성 버튼 노출 + 활성화 1뎁스화 | 계약서 §36(§15-2·§18-9·§20 보정), public/js/{main,ui,renderer}.js, public/index.html, `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 확정 UX 2건. 렌더+UI만, 서버·데이터 계약 불변(activate·bundle API 그대로). ①**숲 생성 노출**: 완료된(미묶음 가장 이른) 달 day-cell bbox 영역 캔버스 딤(펄스 어둠+초록 외곽, `_monthCellGridBounds`·`bundleYm`) + 영역 중앙 DOM "🌳 숲 생성" 버튼(§12, #bundle-btn). 위치는 rAF loop 가 `monthCellClientRect`(월드→백버퍼→캔버스→client 좌표변환)로 매 프레임 추적(팬/줌 따라감)·오버뷰/드릴다운/화면밖 null=숨김. 클릭→`state.bundle`(`'b'` 동일·유지)·read-only 비활성 ②**활성화 1뎁스**: 호버=후보 칸 위 선택 디자인(밝은 박스+픽셀 ＋) 프리뷰(`hoverSlot`), 클릭=후보 1번에 바로 확인 팝업(중간 `selectedSlot` 폐기→`pendingSlot` 확인 동안만)·비후보 칸 아주 옅은 커서(§20 단순 하이라이트 교체)·취소 시 잔여 0. 검증: node --check 3파일·헤드리스 23/23(월 bbox·client 좌표 정합·overview/드릴다운/화면밖 null·세터·render 8프레임 에러0·completedMonthToBundle 회귀·1뎁스 판정). 실모드 육안·위치추적·1클릭은 오케스트레이터 `?cold=1` 후속 |
| 2026-06-15 | §56 상세 모달 구체화·확대 + ESC 우선순위(모달 먼저) + 심기 팝업 확대 | 계약서 §56, public/js/{ui,main,renderer}.js, public/index.html, `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 직접 요청 3건. 렌더+UI(DOM/CSS)+ESC 순서만, 데이터·서버·집계·차단·배치·렌더 파이프라인 불변. ①**상세 모달(#detail) 확대+구체화**: CSS min320/max440·padding16/22·font14·maxH82vh+overflow / `cellInfo` 가 `species`(params 우선·speciesFor 폴백)·`norms`(5 정규화값) 추가 노출(rows 무회귀) / `updateDetail`=날짜+요일(weekdayKR 로컬일)·단계 한글·수종명(SPECIES_NAMES 초록/연두/단풍/진청록/민트)·경험치 바·다음 단계·5메트릭 raw(천단위)+비중`(NN%)`, 빈 대지=요일+안내만 무회귀 ②**ESC 우선순위 역전 해소**: main keydown 확인팝업→상세 모달(`isDetailOpen` display:block 신설)→드릴다운, 모달 열려 있으면 모달만 닫고 return(ESC1 모달·드릴다운 유지, ESC2 드릴다운) ③**심기 팝업(#plant-confirm) 확대**: padding16/20·minW200·제목16·버튼15. 검증: node --check 4파일·실모드 `?mock=1` Chrome 실측(모달 유목·목요일·연두나무·40%·성목60%남음·5메트릭 raw+비중·치수 정합 / ESC 드릴다운2026-01+모달→ESC1 모달만·드릴다운 유지→ESC2 드릴다운 / 심기 팝업 치수 / 빈대지·null 무회귀 / 콘솔에러0) |
| 2026-06-15 | §57 중앙 서버 업로드 — 전송 모듈(서버) + 상태 UI(하단 HUD 한 줄) | 계약서 §57, public/js/{ui,main}.js, public/index.html (+서버 server/{upload,index}.js 는 사전 완성·이 작업 불변), `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 확정 배치. 데이터=서버 `upload.js`(tracker 계약 POST /api/claude-usage/upload multipart·env 의존 UPLOAD_USER_EMAIL/SERVER_URL/INTERVAL·부팅 1회+주기 자동), UI=이 작업(렌더+클라). **상태 UI**: 하단 HUD(`#hud-bottom`) 안 `#upload-row` 한 줄(별도 패널 금지)에 "마지막 전송 x분 전 + 작은 즉시 전송 버튼" 녹임. main 이 `GET /api/upload-status` 30초 폴(+1분 상대시간 타이머)·`/api/forest` 와 독립(?mock/?cold 도 실서버 호출). `ui.relativeTime`(방금/N분/N시간/N일/전송없음)·`updateUploadStatus`(enabled=false→up-off "업로드 꺼짐"·버튼 disabled / lastError→up-err "전송 실패" 빨강)·`onUploadNow`(클릭→`setUploadBusy` "전송 중…"·연타 방지→`POST /api/upload-now`→재폴, 성공 시 "방금 전")·`refreshUploadWhen`. 검증: node --check ui/main/server·서버(env 미설정 enabled:false / env 설정 enabled:true lastError)·실모드 `?mock=1` Chrome(상대시간 4구간+null·에러·꺼짐·busy·버튼 POST 실호출·재폴·`#upload-row` 하단 HUD 배치·무회귀 5메트릭/§56 모달/콘솔에러0) |
| 2026-06-15 | §58 업로드 설정 모달(GUI 설정, 배포용) + §59 미니맵(줌인 시 전체 위치) | 계약서 §58·§59, public/js/{ui,main,renderer}.js, public/index.html (+서버 upload-config API 사전 완성·불변), `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 확정 신규 UI 2건(다른 개발자 배포용). 렌더+클라만, 서버·집계 불변. **§58 설정 모달**: §57 `#upload-row` 톱니(⚙)→`GET /api/upload-config` 로 현재값 채운 모달(`#upload-config-modal` 중앙 딤·#start-modal 톤, 서버주소/이메일/주기초/자동전송 on-off)→저장 `POST /api/upload-config`→모달 닫고 `/api/upload-status` 재폴. 이메일 가벼운 검증(켬+이메일 빔 거부·형식)·interval 최소10. ESC 최우선(설정>확인팝업>상세§56>드릴다운). ui 메서드 7개 신설(onUploadCfgOpen/show/hide/isOpen/_readUploadConfig/onUploadCfgSave/showUploadCfgError). **§59 미니맵**: 별도 캔버스(`#minimap` 우상단 HUD 아래)·`renderer.drawMinimap`(§24 _mapWorldBounds 100×40 축소+활성나무 초록/묶인숲 진초록 점+현재 뷰포트 흰 사각·`pointer-events:none`)·줌인(zoom>zoomMin·!overview) 일 때만 표시(`isOverview` getter·main rAF `updateMinimap`). 검증: node --check 4파일·서버 GET/POST 영속·실모드 `?mock=1` Chrome(설정 모달 톱니열기/GET로드/검증4케이스/ESC닫힘/저장영속+HUD갱신/ESC통합 / 미니맵 줌인표시·맵채움·초록점·뷰포트흰사각·팬추적 cam2602→2902·오버뷰/줌1 숨김·스케일 s0.0278 / 무회귀 HUD·§56·§57·콘솔에러0) |
| 2026-06-15 | §60 UI 위치 조정 4건 + §61 정렬 보정·톱니 확대·미니맵 항상표시 | 계약서 §60(00_arch), public/{index.html,js/{main,renderer}.js}, `_workspace/{00_architecture,20_forest_game_report}.md` | DOM/CSS·main 미니맵 조건만, 서버·기능 불변. **§60(위치 조정)**: ①설정 톱니(⚙) 하단→우상단 HUD(#hud-tr) ②즉시버튼+"x분 전" 묶음 하단 패널 상단 우측 ③미니맵 우상단→좌상단+하늘 포함(§24 _mapWorldBounds 위로 SKY_VIEW) ④미니맵 박스 6:4(=150×100), 땅(5:2) 아래·하늘 위(drawMinimap 하늘 그라데이션+땅 흙톤·뷰포트 사각 하늘 포함 추적). **§61(실모드 스샷 보정)**: ①하단 전송 UI 가 5메트릭 위 올라타던 것 → 패널 **상단 전용 바**(upload-row 흐름배치+우측정렬+구분선, 메트릭은 #hud-bottom-metrics 래퍼) 겹침0·gap6px ②톱니 28×28 확대(font17·클릭 쉽게·util padding 36) ③미니맵 `updateMinimap` 줌인조건 제거→**줌 무관 항상 표시**. 검증: node --check main/renderer/ui·실모드 `?mock=1` 좌표측정(upload-row↔메트릭 gap6·겹침0·baseline일치·톱니28²·미니맵 좌상단8,8 6:4·하늘위/땅아래 landTop40·뷰포트 하늘추적 mn37<40·drawMinimap nonEmpty15000). 한계: 탭 백그라운드(rAF throttle)로 라이브 스크린샷 미확보 — 좌표·drawMinimap 직접호출로 검증, 사용자 활성탭 육안 권장 |
| 2026-06-15 | §62 상세 모달 비중% → 선형(역대 최대 대비) + 가이드 | 계약서 §62(없음·report 기록), public/js/{metrics,renderer,ui}.js, public/index.html, `_workspace/20_forest_game_report.md` | 렌더+UI만, 나무 매핑·logNorm·서버 불변. 상세 패널 %가 logNorm 이라 큰 값 80~95% 몰림(Cache Read 109M/478M 선형23% 인데 log93%)→**상세 표시값만 선형**. ①`metrics.forestCellParams` 가 `linPct`(메트릭별 `clamp01(raw/refFor(refMax,key))`) 별도 계산·params 추가, norms(logNorm)·sim·sap·ground **전부 불변**(나무 높이/잎/금가루/식생 그대로) ②`renderer.cellInfo` linPct 노출 ③`ui.updateDetail` 비중% =linPct·헤더 "사용량 — 실측값 · 역대 최대 대비 %"·하단 안내 `.pct-note` "% 는 관측된 역대 최대 사용량 대비 비율 (최대치는 사용할수록 갱신됨)"(행 호버 refMax 툴팁은 선택→생략). 검증: node --check 3파일·선형표본(109M/478M=23%·600M/478M→100% 클램프·refMax없으면 REF 폴백)·실모드 `?mock=1`(2026-01-22 Cache Read 120.5M/400M=0.30→선형30% vs log94%)·**나무 매핑 무회귀**(maxHeight/leafCount/sap.density/ground.density 전부 norms 그대로·linPct 영향0)·헤더/안내 표시·기존 상세(날짜·단계·수종·경험치·빈대지·null) 무회귀·콘솔에러0 |
| 2026-06-10 | §36 보정: 콜드 목 forests 등록 + 실측 통과 | public/js/state.js | `?cold=1` 픽스처가 일 데이터 생성 시 그 달 forests 엔트리(`{month,bundled:false,...}`)를 안 만들어 숲 생성 버튼이 영영 안 뜨던 결함(실서버 store.js 295행 동작과 불일치) 1줄 보정(오케스트레이터 직접). 실측(`?cold=1` 시작일 2026-05-25): 호버 ＋프리뷰→1클릭 심기 팝업×7→5월 영역 딤+숲 생성 버튼→클릭→숲 군집 전환, 콘솔에러 0 |
| 2026-06-10 | §37 그리드 6종 + 차단 단일 소스 + 숲 살리기·바닥 입체·형태 닫기 + 버튼 상단 | 계약서 §37, public/js/{activation,main,renderer,simulation,particles}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 세션 설계 6건. 렌더+클라 순수함수만, 데이터 계약·일/월/년 API·buildForest/buildTree 반환(closedCells 추가만 하위호환)·베이크·파티클스로틀·activate/bundle 불변. ①**그리드 6종**(신규: 활성불가 테두리·닫힘 = 바위/절벽 바닥 `_drawRockTile`, 비활성 흙에 돌 추가) ②**차단 단일 진실 소스** `activation.blockedSet`(닫힘 4방향 flood-fill ∪ 묶은 숲 4방향 테두리·펼침 해제) → candidateSlots 거름·isAcceptableSlot 거부·렌더 바위 **세 곳 한 집합 참조**·후보0개 자유선택 폴백·인접 우선순위가 차단 못 이김 ③**숲 살리기**(구운 스프라이트 위 라이브: 금가루 weight∝cacheRead log·40캡, 나비 숲당2 리사주, 풀결 sway) ④**바닥 입체**(셀좌표 시드: 얼룩그늘 instances 발자국·톤변주·잡동사니 낙엽/이끼돌/고사리) ⑤**형태 닫기**(buildForest 대각 코너 갭 브리지 셀·closedCells·멤버좌표 불변·§31/§32/§33 정합) ⑥**버튼 상단**(monthCellClientRect cy=상단변, 한가운데 금지). 검증(헤드리스 /tmp): node --check 5파일·activation/simulation 24/24·렌더 스텁 9/9(단일소스 후보∩차단0·floor 팬 mismatch0·버튼 상단·브리지 결정성·정상점증 차단0 무회귀). 실모드 픽셀·육안 오케스트레이터 후속 |
| 2026-06-10 | §36-3 숲 생성 노출 조건 보정(달 완주 검사) | 계약서 §36-1(§36-3 보정 표기), public/js/main.js | 사용자 실측 결함: 오늘 6/10·시작일 5/5·마지막 활성 5/28인데 5월 숲 생성이 이미 노출. 원인=후보 판정이 "달이 지났는가+미묶음"만 검사. `monthFullyPlaced`(그 달 시작일 이후~월말 전 날짜가 placementMap 에 존재) 필터 추가(오케스트레이터 직접, 'b' 단축키도 동일 제한). 실측(`?cold=1` 시작일 5/28): 5월 2~3일치 심음→버튼 없음, 4일치(28~31) 완주→버튼 노출 |
| 2026-06-10 | §37-D·§37-E 보정(브리지 이중추가·나비 조잡) | 계약서 §37(§37-5·§37-3 보정), public/js/{simulation,renderer}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | **§37-D**(integration-qa): 코너 갭 대각쌍이 양쪽 셀에서 두 번 처리돼 직교 연결칸 둘 다 추가→2×2 꽉 참(사각 조각 잔존). 수정=정렬된 쌍 키(`seenGaps`)로 갭당 1회·시드로 택1. 검증 8/8(대각쌍 10/10 연결칸 1개·2×2 0건·한 덩어리·직선성<3셀·결정성). **§37-E**(사용자: 빠름·날갯짓 없음·부메랑): 숲 나비를 빈칸 장식 나비와 **공통 헬퍼 `_drawButterfly`**(펼침 가로2×1↔접힘 세로1×2 토글·몸통·파스텔)로 추출·진폭5px(≤6)·주파수0.05. 검증 9/9(2자세 토글·진폭≤6·결정성·무회귀) |
| 2026-06-11 | §37-F 보정(닫힘/활성불가 시각 분리 + 차단 외곽 유기화) | 계약서 §37-F(§37-1·§37-2 보정), public/js/{activation,renderer}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 실모드 스크린샷 2건. 렌더만 불변. **결함1**(닫힘=활성불가 둘 다 회청 바위): `blockedSet` 반환 Set→**Map(tag)**·닫힘 flood-fill 먼저(enclosure→`closed:ym` 둘러싼 달 다수결)·활성불가는 닫힘 아닌 바깥만 `blocked`·`candidateSlots`/`isAcceptableSlot` `.has` 호환(동작 불변). 렌더 `_blockedKind` 분기: 활성불가=`_drawRockTile`(회청), 닫힘=PAST 풀톤+`_drawClosedGround`(옅은 풀+이끼). **결함2**(바위 직각 vs 숲 곡선): `_drawBlockedEdges`(!lod) 바깥 변만 §32 침식 스캔라인(이웃 배경색 `_neighborBaseColor`)·내부 변 이음매0·칸좌표 시드. 검증 스텁 18/18+회귀 8/8(Map 태그·닫힘 우선·색거리>60·외곽 침식 직선아님·팬 mismatch0·단일소스 무회귀)·node --check |
| 2026-06-11 | §37-G 보정(바닥 입체·경계 침식 과함 완화) + §37-H 입체 바위 장식 | 계약서 §37-G·§37-H, public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | **§37-G**(사용자 스샷 3건, "과해서 인위적"→덜어내기): `_drawForestFloor` 큰 톤 패치(0.3~0.65×GRID) 폐기·잡동사니 3~6→1~3·얼룩 그늘 나무 인스턴스 아래만 작은 타원(반경≤GRID/4·alpha 0.14·빈칸0) / 경계 침식 모듈 상수 `EROSION_E=GRID*0.07≈4`·`EROSION_AMP=0.45`+공통 `edgeWaveLen`(len 2~6px, 이전 0~12 과출렁임), `_drawForestFloor`·`_drawBlockedEdges` 공유(직각 아님·완화). 검증 13/13(큰패치0·그늘 나무y근방·침식 max-min≤6·팬 mismatch0). **§37-H**(사용자 요청 입체 바위): `_drawVolumeRock`(타일0.7~0.85배·접지그림자+돔음영·2변형) 빈 땅 ~6% 듬성·차단/나무 발치 제외·Y-sort 편입. §26-2 버그 자체발견(절대 yy패리티 색뒤집힘→밑동기준 행변주). 검증 15/15(폭<타일·입체·밀도≤0.3·차단0·나무발치0·팬 mismatch0). node --check |
| 2026-06-11 | §38 생태계 보강(끊긴 메트릭 경로 잇기) + §37-I 숲↔바위 흙 비침 | 계약서 §38·§38-7, public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 메트릭 점검(Explore) "렌더 절반 죽음" 근거, 끊긴 것만 잇기. 렌더만(activation·simulation·particles·data·server·월density·차단단일소스 불변). **§38-1**(요청수→발치 식생, 끊김 해소): `ground.density=requestN` 소비처0 → `_cellGroundDensity`+`_drawDecor(density)` 식생 개수 density 비례(한산0~1·북적2~3, 큰패치 금지). **§38-2** 식생 다양성: `_drawDecorItem` 분리+버섯·클로버·갈대(7종)·숲바닥 버섯. **§38-3** 물웅덩이 `_drawWaterPool` 빈땅 ~0.9% 희귀·Y-sort·**activation 무관**(못심는칸 안만듦). **§38-4**(누락버그) 일성목 금가루 `weight=clamp(0.3+cacheReadN*1.7,0.3,2)`·40캡. **§37-I**(스샷): 묶인숲 칸 베이스=흙→`_drawForestFloor` E인셋 E링에 흙비침, `_isBundledTile`로 베이스 PAST풀톤·디테일 흙생략(`_neighborBaseColor` 원래 정확·무회귀). 검증 18/18+무회귀3/3(요청수단조·종류≥3·물 mismatch0·weight·40캡·숲footprint흙0·§37-G큰패치0)·node --check·서버 200 |
| 2026-06-11 | §39 놓인 바위 스프라이트 교체(§37-H 절차→사용자 시트)+크기 자유·바위군 | 계약서 §39, public/js/{sprites,renderer}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(데이터·서버·경계면·activation/blockedSet 불변, 서버 기존 .png MIME). **로더**: `sprites.js` `RockSheet`(나무 SpriteSheet 패턴 재사용·`frameByIndex`/`count`)+`rockSheet` 싱글턴·5프레임 `sprite_0_0`~`4`·비동기·실패 폴백. **시트 blit**: `_drawVolumeRock` 재작성, `_rockSprite(frameIdx,targetW)` nearest 정수스케일 베이크(캐시 LRU64)→drawImage(밑동 하단중앙 앵커·픽셀퍼펙트), 미준비/실패=`_paintProceduralRock`(§37-H 본체 폴백). **크기 자유** 0.5~2타일(주1.0~2.0·위성0.5~1.1)·5프레임 시드택1. **바위군** 60%단일·40% 2~4군집(발치 작은돌·섞임·back→front). **배치**: 빈땅 ~6% 듬성·차단/나무발치/§38 물웅덩이 겹침 제외·칸좌표시드·Y-sort. 검증 20/20+무회귀3/3(절차폴백·시트blit·5프레임·정수dw/dh·캐시적중·크기0.5~2·바위군2~4·차단0/나무0/물0·팬mismatch0·blockedSet무회귀)·node --check·rock.png 200 image/png |
| 2026-06-11 | §39-A·B 보정(바위 크기 ≤ 묘목 + 접지 그림자) | 계약서 §39-A·§39-B, public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 사용자 실모드 피드백. 렌더만 불변. **§39-A**(크기 ≤ 묘목): §39 0.5~2타일이 큼. `_saplingRenderHeight()`(묘목 시트 h×niceScale·미준비 footprintWidth(SAPLING)≈14 폴백) 신설, `_rockSprite` **폭→높이 정규화**(세로 바위 종횡비 대응)·목표 높이 주0.75~1.0·위성0.4~0.7×묘목=전부 ≤묘목·캐시키 frameIdx\|targetH·절차도 rh*2≤묘목. **§39-B**(접지 그림자): 투명 배경이라 없던 그림자, `_drawGroundShadow`(밑동 아래 타원 rgba(20,40,24,0.20)·§37-G 톤 정합·밑동폭 비례) blit 직전·멤버마다. `_paintProceduralRock` 시그니처 rh 추가·내부 그림자 제거. 검증 21/21(개별 바위 높이≤묘목 초과0·그림자 존재·5프레임≤묘목·정수스케일·캐시적중·팬mismatch0·배치 무회귀)·§37-I strict bbox 흙0·node --check |
| 2026-06-11 | §40 풀↔흙 경계 = 월드 노이즈 전이대(사용자 직접 승인) | 계약서 §40, public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(데이터·서버·activation/blockedSet 불변). **1차 범위=활성 풀밭↔비활성 흙 경계만**(바위§37-F·묶인숲 외곽·`_drawBlockedEdges` 무변경→§37 제약 자동 보존). `_vhash`/`_vnoise`(smoothstep)/`_fbm`(2옥타브) 월드 노이즈(팬 불변)·`_warpedGrassDirt(u,v,amp)` 도메인 워프 분류(amp 1.2타일·범위밖 crisp 폴백)·`_grassDirtKindCell`(활성grass·평범 비활성dirt·차단/묶임 null)·`_isGrassDirtBoundaryTile`. 베이스① 경계 타일만 per-2px 블록 워프(STEP2 도트정수)·비경계 solid. 결② `_drawTransitionDetail`(점마다 워프 분류). 베이크 `!lod`만. 검증 17/17(경계판정·칸주기성제거·워프 칸가로지름·**팬불변 mismatch0**·도트정수·바위/숲/차단 무회귀·§37-I 숲흙0·blockedSet무회귀·render0)·perf 5활성칸 ~51k fillRect/frame·node --check |
| 2026-06-11 | §39-C 보정(바위 자갈밭 완화) + §41 마지막 활성 그리드 찾기 버튼 | 계약서 §39-C·§41, public/js/{renderer,sprites,ui,main}.js·index.html, `_workspace/{00_architecture,20_forest_game_report}.md` | **§39-C**(실모드 자갈밭): 바위 밀도 ~6%→~2.8%(절반↓)·프레임 가중 `RockSheet.weightedIndex`+`ROCK_WEIGHTS=[42,27,17,10,4]`(작은 돌 多·큰 바위 少)·크기 주0.5~0.75·위성0.3~0.5×묘목. 검증 13/13(밀도≤3%·분포 단조감소·크기↓·그림자/군집/배치/결정성 무회귀). **§41**(지도앱 내위치): 렌더+UI만·카메라만(게임상태 불변). `renderer.focusLastActive()`(`_lastActiveCell` 재사용→화면중앙+줌인 ~3.5칸폭·맵클램프·오버뷰해제) + 부드러운 보간 `_camAnim`/`_stepCamAnim`(ease-out 0.4s·줌 정수스텝 픽셀퍼펙트) + `hasLastActive()`. UI `#locate-btn`(📍 우하단)·`ui.updateLocateButton`/`onLocate`·main 배선. 검증 20/20(콜드숨김·중앙±1칸·줌인·클램프·보간 즉시아님·오버뷰복귀·드릴다운/placement/blockedSet 무회귀·render0)·node --check·서버200 |
| 2026-06-11 | §42 그리드 격자선 제거(디버그 모드 전용) | 계약서 §42(§22·§35 보정), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만 불변. §40 노이즈 경계 위 칸 외곽선이 격자 재노출→인위적. `_drawGridBorders` **맵 경계선+칸 1px 외곽선**을 `if(this.debug)` 로 묶어 기본 OFF·`?debug=1` 만(§35 가드 재사용). **유지(블록 밖)**: 숲생성 딤·후보강조·호버 프리뷰·커서 하이라이트·빛기둥·§41 포커스. 검증 11/11(기본 격자픽셀0·debug=1 표시·딤/호버/커서/빛기둥 무회귀·§41/§40/결정성/render0)·node --check·서버200 |
| 2026-06-11 | §43 마지막 활성 오라 마커 + §44 바위 경계 노이즈 통일 | 계약서 §43·§44, public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(데이터·서버·배치·blockedSet 불변). **§43**(사용자 정정: 핀→오라): `_drawLastActiveAura`(render 패스)·`_lastActiveCell` 밑동서 피어오르는 따뜻한 빛(글로우+상승 입자+테두리 맥동, frame 사인)·**떠있는 도형 마커 없음**·plantFx 와 구분되는 지속·약함·이동 따라감·콜드 없음·팬 불변. **§44**(바위 격자 네모): §40 `_grassDirtKind*`→**`_groundKind*`(grass/dirt/rock/closed/null)** 4종 일반화·활성불가=rock·닫힘=closed 편입·묶인 숲만 null(§37-I). `_isGroundBoundaryTile`/`_warpedGroundKind`/`_groundBaseColor`. 베이스/결/`_drawTransitionDetail` 4종 확장·**바위 회청·닫힘 PAST 풀톤 유지·경계만 노이즈**·`_drawBlockedEdges` 호출 폐기. 검증 24/24(오라 마커·이동·도형0·rock 편입·가장자리 노이즈/중앙solid·회청색 유지·blockedSet 무회귀·팬 mismatch0·§40/§41/§42·결정성·render0)·node --check·서버200 |
| 2026-06-11 | §46 묶인 숲 외곽 노이즈 통일(경계 통일 완료) | 계약서 §46(§40·§44 확장), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(데이터·서버·배치·blockedSet·buildForest·베이크 불변). 지시 번호 §45 충돌(§45=기존 메트릭→식생 매핑 기획 점유) → **§46 으로 기록, 기존 §45 보존**(복원 불필요 — 문서 미작성 상태였음). §44 가 묶인 숲만 null 제외라 숲 바닥만 칸 직각(사용자 "숲 경계 어색"). **`_groundKindCell` bundled→"forest"**(null 없음)·forest·closed 동일 PAST 풀톤·땅 베이스 패스가 숲 경계 per-block 노이즈. **`_drawForestFloor` base fill·§32 외곽 침식 폐기**(이중 처리 방지)→내부 질감만. 숲 내부 풀톤/풀잎/군집/§37-I 흙비침0 불변. 검증 19/19(forest 편입·둘레 노이즈/중앙solid·PAST 풀톤 유지·base fill 폐기·숲 footprint 흙0·팬 mismatch0·blockedSet/§40/§44 무회귀·render0)·node --check·서버200. **모든 바닥 경계(풀·흙·바위·숲) 노이즈 통일 완료** |
| 2026-06-11 | §45 메트릭→발치 식생 매핑 + refMax/species 소비(렌더+클라 구현) | 계약서 §45-8 채움, public/js/{metrics,state,renderer}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더+클라만(서버·집계 불변, 데이터레이어 refMax영속·species저장은 metrics-engineer 병행). **나무 변조 폐기·발치 식생 분산**(§28 프리즈 방지). **§45-1~3**: `_drawDecorItem` 종 id 명시·`_pickVegSpecies`(나비 8% 별도→그룹 확률 input/output/cacheWriteN 비중→종 택1·sum0 균등 폴백)·`_cellVegNorms`(멱강조 norm^2)·개수는 requestCount(§38-1) 유지·타일시드 팬불변. **§45-5** 자갈=cacheWrite: 바위 배치 임계 활성칸만 cwN 가변(`_cellCacheWriteN`·비활성 고정·저장0). **§45-4** refMax: `forestCellParams(day,cap,refMax)` 상수REF→top-level refMax 분모(키별 폴백)·state 가 resp.refMax 전달. **§45-7** species: params.species=tree.species·renderer 2경로 데이터값 우선·없으면 speciesFor 폴백·군집은 speciesFor 유지. 베이크 키 형태 동일→§28 프리즈0. 검증 20/20(refMax 분모/폴백·species 데이터값/폴백·그룹 단조 cacheWrite축적·output결실·input양분·나비8%·sum0 균등·발치 식생 팬mismatch0·자갈 가변/고정·§28 재베이크폭증0·§38~§46 무회귀·render0)·node --check·서버 refMax 필드명 정합·tree.species 미반영시 폴백. 결정성 미세차=ambient 파티클 Math.random(§16-2 의도, §45 무관). 경계면 integration-qa 후속 |
| 2026-06-11 | §47 닫힘 그리드 활성화 버그 수정(둘러싼 달 집합) | 계약서 §47(§37-F 보정), public/js/{activation,main}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 클라 순수함수만(데이터·서버·배치·렌더 불변, 허용 판정만). 버그: 닫힘 칸이 차단 집합이면 태그 무시 무조건 거부 → "둘러싼 그 달만 배치 가능" 정의 위반·다수결 단일 달만 박아 복수 달 둘러쌈(활로) 놓침. **`blockedSet` 닫힘 태그 `closed:M1,M2,…`**(둘러싼 달 전부·대표톤 다수결 첫 항목)·`closedMonths`/`canPlantMonthAt` 헬퍼·`isAcceptableSlot`/`candidateSlots` 에 pendingMonth 인자(닫힘=그 달이면 허용·blocked 무조건 거부)·main pendingMonth() 3 호출부 전달. 심으면 점유→다음 폴 자동 해소. 렌더 `closed:` 접두만 봐 완전 하위호환. 검증 23/23+렌더 4/4(단일달 5월허용/6월거부·복수달 {5,6} 둘다허용/7월거부·활성불가 무조건거부·candidateSlots 닫힘 그달포함/blocked제외·심은뒤해소·closedMonths파싱·단일소스일관·렌더 closed kind 무회귀·render0)·node --check |
| 2026-06-11 | §48 경계 그라데이션 디더(전 경계) + §49 풀 굽이침·바위 위 풀 제거 | 계약서 §48·§49, public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(데이터·서버·배치·차단 불변·사용자 직접 승인). **§48**(사용자 "그라데이션 섞임"): §40 전이대에 거리 기반 도트 디더 `_ditheredGroundKind(u,v,amp,dith)`=워프점+블록별 지터(`_vhash` 월드시드)→경계 근처 두 종류 50:50 도트·먼 영역 순수. 베이스①·결②(`_drawTransitionDetail`) 공통·`TRANS_DITHER=1.0`·**전 경계(풀↔흙·바위·숲·닫힘) 일관**·§37-F 톤(회청 vs 풀톤) 구분 유지·팬불변·정수. **§49**(사용자 "풀 굽이쳐야"·"활성불가 위 풀 어색"): `_drawBentBlade`(밑동 고정·끝 최대 y비례 휨·프레임 사인)→`_drawForestFloor`·§38 풀무더기·갈대 / `_drawTransitionDetail` 자기칸 rock 이면 풀결 텍스처 0(베이스 그라데이션만)·닫힘 풀 유지·`_drawDecor` active만(무변경). 검증 23/23(4종 경계 디더·먼영역/중앙 순수·팬mismatch0·정수·톤구분·끝휨>밑동(밑동0)·프레임변화·결정성·바위 풀결0·닫힘 풀유지·§40~§47/§45 무회귀·render0·렌더 결정성 파티클제외)·node --check·서버200 |
| 2026-06-11 | §50 활성화 후보 앵커 = 마지막 활성 그리드(달력 어제 폐기) | 계약서 §50(§15-2·§18-9 보정), public/js/{activation,main,renderer}.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 클라 순수함수만(데이터·서버·배치·렌더 불변·사용자 승인). 버그: `candidateSlots` 앵커가 달력 어제(`prevYMD`)라 심는날(oldestPendingDate)과 어긋남·밀리면 엉뚱한칸·어제 안심으면 모든 점유칸 8방향 헐거움. 앵커=**마지막 활성 그리드**(§41 `_lastActiveCell` 통일)·`candidateSlots(…,anchorGrid)`·main `renderer.lastActiveGrid()`(신설) 전달·prevYMD 분기 폐기·anchorGrid 없으면 `latestPlacement` 폴백·8방향 전부 차단→후보0 자유폴백·콜드 빈배열·isAcceptableSlot/§47 유지. 검증 13/13+정렬4/4(마지막칸 8방향·옛칸옆0·밀린경우 일관·폴백·8방향차단→자유·콜드0·§47유지·lastActiveGrid==§41 _lastActiveCell·render0)·node --check |
| 2026-06-11 | §51 마지막 활성 표식 재정정(오라→붉은 깃발) | 계약서 §51(§43 폐기), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(데이터·서버·차단 불변). §43 오라(글로우 스캔라인)가 스팟라이트 원뿔처럼 퍼져 어색 → 폐기. `_drawLastActiveAura`→**`_drawLastActiveFlag`**: 밑동 사선 깃대(짙은갈색 #4a3b2a) + 꼭대기 붉은 천(펜넌트 #e23434/#a82828 음영)·**천만 frame 사인 펄럭·깃대 고정**·도트정수·작게·칸 위상 팬불변·오버뷰 작게·콜드 없음·plantFx 별개. 오라 3요소 전부 폐기. 검증 14/14(콜드 없음·깃대 사선+붉은천·**오라0**·천 펄럭·깃대 고정·_lastActiveCell 추적/이동·결정성·plantFx 독립·render0·§50/§41/§48/§49 무회귀)·node --check·서버200 |
| 2026-06-11 | §52 깃발 위치 보정(밑동 발치·인스턴스 배치·단계별 수관 회피) | 계약서 §52(§51-1 보정), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(`_drawLastActiveFlag` 위치계산만, 데이터·서버·차단 불변). 사용자 실모드: 깃발이 **나무 수관 한가운데 박혀** 잎에 묻힘(수관 높이에 그려짐). 셀 중심 고정 폐기→그 나무 **인스턴스 실제 배치**: 접지점 `footY=oy+gy*GRID+off.y*TILE+5`(=`_drawTree` baseY, `off=placement.offsetTiles`)에 깃대·밑동 **왼쪽**으로 `crownHalf(_footprintWidth(stage)*0.5)+sideMargin+flagW`(천이 오른쪽 펄럭여도 **수관 실루엣 밖**·성목=더옆·묘목=가까이)·`poleH=GRID*0.42`(밑동~허리, 수관 안올라감)·**EMPTY=셀 중앙 바닥**·render순서상 나무 위(앞). §51 디자인(사선깃대·붉은천·펄럭·고정·추적·콜드없음·plantFx별개·칸위상 팬불변) 유지. 검증 15/15(깃대 밑동y=접지점·수관중앙높이 깃발0·**단계별 수관 x-겹침0**·성목>묘목 옆거리·EMPTY 셀중앙·펄럭·깃대고정·결정성·추적·render0·팬불변)+회귀 §50/§41/§48/§49 5/5·node --check 11파일·서버200. 실모드 육안 오케스트레이터 후속 |
| 2026-06-11 | §53 나무 잎 라이브 오버레이(흔들림·햇빛 반짝·낙엽) | 계약서 §53, public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(`LEAF_PAL`/`LEAF_HI`/`leafPalFor` 상수 + `_drawLeafOverlay` 신설 + render() 1줄, 데이터·서버·차단·**베이크 불변**). 번호 정정: 같은 날 §52가 깃발 보정으로 먼저 기록돼 잎 오버레이를 §53 통합(§52 깃발 코드 불변·중복 0). 베이크 본체 위 **라이브 덧그림**(재베이크0·§28 프리즈0·색은 상수 테이블만, 스프라이트 픽셀 샘플 금지). 대상=**활성 그리드 개별 나무 YOUNG·MATURE**(SAPLING 생략·EMPTY 제외·과거(isActive 아님) 제외·**묶인 숲 군집 제외**=§37 금가루·나비 담당)·화면 컬링·밑동=`_drawTree` 동일식. ①**흔들림**: 수관 가장자리 잎 3~6개 frame 사인 1px 살랑·`LEAF_PAL[species]`(0초록~4민트) 2톤·칸 시드 위상(팬불변) ②**햇빛 반짝**: 수관 좌상 1~2점 가끔·짧게(140f 주기 8f 창)·`LEAF_HI`·과밀0 ③**낙엽**: 슬롯2(**동시≤2**)·지그재그 하강→바닥 페이드·빈도캡(380f 중 120f)·수종색·frame+타일 시드(결정적·Math.random 없음·팬불변). render 숲나비 다음·깃발 전. 검증 16/16(대상 필터 MATURE>0·SAPLING/EMPTY/과거/묶인숲 0·잎색=LEAF_PAL[species]∪HI·흔들 1px·반짝 가끔·과밀0·낙엽 동시≤2·**베이크 캐시 불변**·결정성·팬불변·render0)+회귀 §52 15/15·§50/§41/§48/§49 5/5·node --check 11파일·서버200. 실모드 육안 오케스트레이터 후속 |
| 2026-06-11 | §52 재보정: 깃발 = 칸 우하단-5px 고정 + Y-sort 편입 | 계약서 §52(§51-1 보정·확정 사양), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(`_drawLastActiveFlag` 위치 + Y-sort 큐, 데이터·서버·차단 불변). 사용자 실모드 2차: 1차 수정안(밑동서 `crownHalf` 옆 오프셋)이 **성목 수관 커서 깃발을 옆 칸(흙)까지 밀어냄**. → ①**수관 회피 오프셋 폐기**(`crownHalf`·`_footprintWidth`·`sideMargin` 제거) ②밑동 = **칸 우하단 꼭지점 안쪽 5px**(`footX=cellR-5`,`footY=cellB-5`) **고정**(나무 형태 무관·EMPTY 동일) ③**칸 안 클램프**(천 우측 펄럭 우경계 넘으면 `cx=min(cellR-5,cellR-1-clothReach)`)·`poleH=GRID*0.4` ④**Y-sort 편입**(사용자 "sort 계산을 해야 한다"): `objs` 큐에 `{kind:"flag",y:footY}` push+dispatch, `render()` "항상 위" 별도 그리기 **폐기** → 접지보다 앞은 깃발 가리고 뒤는 깃발이 가림(깊이 정합). 유지: 사선 깃대·붉은 천 펄럭·깃대 고정·`_lastActiveCell` 추적·콜드 없음·팬 불변. 검증 30/30(밑동 y=칸 우하단-5px·밑동 x=클램프값 우경계 안·**전체 픽셀 칸 안 240프레임 벗어남0**·EMPTY 동일·crownHalf/_footprintWidth 제거·**flag objs push+dispatch**·render() 깃발 별도호출0·깊이=footY키·펄럭·깃대고정·결정성·추적·render0·팬불변)+회귀 §53 16/16·§50/§41/§48/§49 5/5·node --check 11파일·서버200. 실모드 육안 오케스트레이터 후속 |
| 2026-06-11 | §52 좌표 버그 수정: 칸 bbox = 중심±GRID/2(gridToScreen 규약 정정) | 계약서 §52-4(§52-1 정정), public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(`_drawLastActiveFlag` bbox 식 + 큐 push y + 스냅샷 가드, 데이터·서버·차단 불변). 사용자 실모드: 깃발이 나무 칸에서 **2~3칸 떨어진 흙칸**(헤드리스 30/30 통과하고도 어긋남). **근본**: `gridToScreen(gx,gy,ox,oy)=ox+gx*GRID` = **칸 중심**(grid.js 주석)인데 §52-1 깃발 코드가 **좌상단**으로 보고 `cellR/cellB=+GRID` → 우하단을 중심+GRID(≈1.5칸 우하)로 계산 → 옆 흙칸. 라이브 ox/oy 는 나무와 동일(좌표계 불일치 아님·bbox 규약 오류). **테스트 결함**: 옛 헤드리스가 기대 bbox 를 렌더러와 **같은 잘못된 식**으로(자기참조) + `_drawLastActiveFlag` 만 직접 호출 → 함께 틀려도 통과. **수정**: 칸 bbox=**중심±GRID/2**(`cellCx=ox+gx*GRID`,`cellL=cellCx-GRID/2`…)·큐 push `fy=oy+gy*GRID+GRID/2-5`·깃발 **라이브 패스 전용**(push `!_snapComposite` 가드, 맵 스냅샷 합성 제외). 위치 사양·디자인 불변, 좌표계만. **테스트 정정**: 신규 `forest52_realpath.mjs`(7/7, **full render() dispatch**·기대 bbox=gridToScreen 중심±GRID/2·깃발 전체 픽셀 활성칸 안·팬 후 같은칸·합성패스 깃발0)·`forest52.mjs` 규약 정정(30/30, 자기참조 제거). **실모드**(`?mock=1&debug=1`, claude-in-chrome): 깃발 나무 칸 바닥·드래그 팬 후 같은 칸 추적·콘솔0. 검증 실경로7/7·규약30/30·§53 16/16·§50/§41/§48/§49 5/5·node --check 11파일·서버200·실모드 픽셀 확인 |
| 2026-06-11 | §54 활성 풀밭 초록 3톤 결(흙 pendDirt 3색 방식) | 계약서 §54, public/js/renderer.js, `_workspace/{00_architecture,20_forest_game_report}.md` | 렌더만(팔레트 `grassC`/PAST `grassB·C` + 활성 타일 base, 데이터·서버·차단·경계분류 불변). 활성 풀밭 grassA 한 톤 단색 → **초록 3톤**(grassA/B/C 미세 명도차)을 **비활성 흙 pendDirt 3색과 같은 per-타일 월드 해시**(`v=((tx*P1)^(ty*P2))%3`)로 덮음. `PAL.grassC=#5dab40` 신설(스프레드<12)·PAST grassB/C(묶인 숲·닫힘 base 일관, blade2/blade1 넓은대비→A/B/C 미세). 활성 타일 `continue`→3톤 fill 1회·풀잎 결 그 위. **§37-G 무회귀**: 칸 단위 큰 패치(0.3~0.65×GRID center-falloff) 0·per-타일 랜덤·타일 단위 fill·월드 시드 팬불변·LOD/스냅샷 결 생략. §40~§46 경계분류 무관(base 색만). 검증 10/10(3톤 모두 등장·연속런 짧음·편향<0.45·스프레드<12·변주 큰패치0·팬불변 mismatch0·결정성·LOD생략·render0)+**실모드**(`?mock=1` claude-in-chrome: 활성·묶인 숲 바닥 미세 3톤·큰패치없음·콘솔0)+회귀 §52실경로7/7·§52규약30/30·§53 16/16·§50/§41/§48/§49 5/5·node --check 11파일 |
| 2026-06-11~15 | §55 자동 숲 묶기 + 활성 불가(blocked) 폐기 + 빛기둥 | 계약서 §55, server/{store,mock}.js, public/js/{renderer,activation,main,ui,state}.js·index.html, `_workspace/{00_architecture,20_forest_game_report,30_qa_report}.md` | 사용자 확정 발화("월 경계 넘으면 자동 숲", "활성 불가 제거—바로 옆 심기", "완주 무조건", "빛기둥"). 데이터·집계·refMax·species·**닫힘(closed §47)** 불변. ①**자동 묶기**(서버 `autoBundlePastMonths`: boot+폴마다, 지난 달 무조건 bundled·완주무관·month.json 영속·이미묶음 재생성0) ②**숲 생성 버튼·수동 묶기 UI 폐기**(#bundle-btn·bundleYm·completedMonthToBundle·state.bundle·'b' 제거) ③**활성 불가(blocked) 완전 폐기**(blockedSet→**닫힘만**·숲바깥 인접 후보/심기 허용·rock 렌더·_drawRockTile 호출 폐기, 닫힘 §47 유지) ④**빛기둥**(main false→true 감지→playBundleEffect·bundleFx dur48). **메인 직접 단위검증**(integration-qa 두 차례 중단[한도·소켓]): node --check 클라·서버 전부 / mock 지난달 5개 전부묶임·현재월 미묶음 / blockedSet blocked태그**0**·closed 1·숲바깥 비차단 / 숲바깥(4,5) 6월 심기**허용** / 닫힘(6,6) 5월허용·6월거부(§47) / playBundleEffect·bundleFx·main감지 배선. 실모드 육안(버튼없음·바위사라짐·자동숲·빛기둥) 후속 |
| 2026-06-17 | §63 나무 단계 분모 교체(일일누적 역대최대 dailyMaxTokens) | 계약서 §63, server/{aggregate,store,mock}.js, public/js/cold-source.js, `_workspace/{00_architecture,10_metrics_engineer_report}.md` | 순환 결함 수정: 기존 분모 `dailyCapTokens=2×fiveHourCapTokens`(statusline 역산)가 분자·분모 동일 입력 공유 → pct≈(블록수×블록사용률)/2 로 사용 강도 약분, 많이 써도 단계 안 오름. **단계 분모만** 일일누적 역대최대(`dailyMaxTokens`)로 교체(refMax 동일 룰: DAILY_MAX_SEED 시드하한·단조증가·부팅 전체스캔·폴 bump·forest.json 영속). computeTree 임계(young0.3/mature0.6) **불변**(인자명 dailyCapTokens→denomTokens, 호출부가 mem.dailyMaxTokens 전달). **HUD cap 분리·보존**(top-level dailyCapTokens/fiveHourPct/fiveHourCapTokens/capSource 는 estimateCaps 그대로 — 단계 판정과 HUD 표시가 다른 분모, 의도). **식생 refMax 불변**(메트릭별 logNorm·나무 높이/잎/금가루/발치식생 그대로). **과거 finalized 동결 유지**(freezeStaleDays, 오늘 이후만 새 분모). mock(MOCK_DAILY_MAX_TOKENS 700M)·cold(MOCK_DAILY_MAX 494.85M) 메타 노출. 클라 무변경(metrics.js 가 서버 tree.stage 그대로 받음, cap 으로 단계 재계산 안 함). 검증: node --check 전파일 / 헤드리스 22/22(/tmp 격리, 실 data 불가침): (a)단조·시드·전체스캔·재부팅단조·영속·노출 (b)분모=dailyMaxTokens·임계0.3/0.6(누적=dailyMax→pct1.0성목·0.5×→young·0.7×→mature·경계·0→empty) (c)HUD cap 불변·단계분모≠HUDcap (d)refMax 5키·시드하한·미혼입 (e)과거 finalized·stage 재계산0 |

## 하네스: 아키텍처 감사·교정

**목표:** 코드가 각 모듈의 속성(순수데이터/순수함수/상태/IO/렌더/UI/조율)과 사용 목적에 맞게 자리잡았는지 검증하고, 어긋나면 이 프로젝트에 알맞은 아키텍처를 제시·안전 교정한다. 빌더 하네스(`claude-forest-orchestrator`)와 별개 — **구조를 본다.**

**트리거:** "아키텍처 검증/감사", "리팩토링 검증", "구조 점검", "속성·목적에 맞는지", "구조 개선", "모듈 분리 검증" 요청 시 `architecture-refactor-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**안전망:** 렌더 비교 시험 기준값(`test/render-smoke.test.js`, 현재 `7c022c6`) + `npm test` 26케이스 + 브라우저 스모크. 교정은 이 가드 아래에서만.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-16 | 초기 구성 | agents(architecture-reviewer·refactor-surgeon 신규, integration-qa 재사용) + skill(architecture-refactor-orchestrator) | 속성·목적 정합 검증·교정 하네스 신규 구축 |

## 하네스: Electron 데스크톱 배포

**목표:** 기존 웹 대시보드(`server/` + `public/`)를 Electron 으로 감싸 윈도우·맥 배포본을 만든다. 기능을 새로 만들지 않고 **이미 동작하는 앱을 데스크톱으로 포장**한다. 메인 프로세스가 기존 http 서버(포트 5178)를 띄우고 BrowserWindow 가 localhost 를 로드.

**트리거:** "Electron/일렉트론", "데스크톱 앱", "윈도우 앱", "맥 앱", "exe/dmg", "설치본·배포본 만들기", "앱으로 패키징" 요청 시 `electron-distribution-orchestrator` 스킬을 사용하라. "다시 빌드", "타겟 추가", "win 도 빌드" 같은 후속 요청에도 사용. 단순 질문은 직접 응답 가능.

**확정 전제:** 코드 서명 없이(개인/사내) · 로컬에서 각 OS 직접 빌드 · 자동 업데이트 제외. 바꾸려면 사용자 재확인.

**핵심 함정:** `server/store.js` 의 `data/` 쓰기 경로는 패키징되면 asar(읽기 전용) 안이라 쓰기 실패 → 메인이 `app.getPath('userData')` 를 `FOREST_DATA_DIR` env 로 주입, `store.js` 가 env 우선(없으면 개발 경로 폴백). 개발 무변경, 배포본만 userData.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-16 | 초기 구성 | agents(electron-integrator·packaging-engineer·build-verifier 신규) + skills(electron-app-integration·electron-packaging·electron-build-verification·electron-distribution-orchestrator) | win/mac 데스크톱 배포 하네스 신규 구축. 서버 통합→패키징→구동검증 서브 에이전트 파이프라인 |
| 2026-06-16 | 1차 실행 완료(전 단계 통과) | main.js·preload.js 신설, server/{index,store}.js(startServer 추출·FOREST_DATA_DIR env), package.json(main·build·dist scripts·electron 42/electron-builder devDeps), `_workspace/5{0,1,2}_*.md` | Electron 통합→패키징→검증. mac 배포본 `dist/*.dmg`·`*.zip` 산출·구동·data userData 쓰기·`npm test` 31/31 회귀 통과. win 미검증(윈도우 머신 `npm run dist:win` 필요)·GUI 숲 육안 사용자 위임 |

## 하네스: UI 디자인·완성도 점검

**목표:** UI 형태·완성도·배치가 기획 의도(`_workspace/00_architecture.md` §3·§5·§7 + `20_forest_game_report.md` 최근 사양)에 부합하는지 **라이브 화면으로** 점검하고 교정한다. 코드 구조를 보는 아키텍처 감사 하네스와 별개 — **렌더된 픽셀을 본다**(claude-in-chrome 캡처가 본질, 코드만 보면 겹침/가림을 놓침).

**트리거:** "UI 점검", "UI 검토", "완성도 점검", "배치 점검", "기획 부합 점검", "디자인 검토", "화면 점검" 요청 시 `ui-design-review-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-17 | 초기 구성 | agents(ui-design-reviewer 신규) + skills(ui-design-review·ui-design-review-orchestrator), forest-game-builder 교정 재사용 | "배치하고 검토 안 하냐" 교훈 — 라이브 화면 캡처·기획 대조 점검 하네스 신규 구축 |

## 하네스: 코드 기준 기획문서화 (역설계 spec)

**목표:** 코드가 기획문서를 앞질러 생긴 drift 를, **코드를 진실로 삼아** 상세 기획문서를 다시 길어 올려 해소한다. 영역별로 코드를 역설계해 사실 조각을 추출하고, 하나의 통합 기획문서로 합성한 뒤, 문서↔코드 정합을 교차 검증한다. 기존 `00_architecture.md`(기획 의도·계약서)는 히스토리로 보존하고, 코드 현 상태를 기술하는 새 문서를 만든다.

**트리거:** "코드 기준 기획문서", "기획문서 작성/갱신", "코드 역설계 문서", "spec 작성", "코드와 기획 동기화", "drift 정합", "현재 코드 정리해서 문서로" 요청 시 `spec-from-code-orchestrator` 스킬을 사용하라. 단순 질문은 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-17 | 초기 구성 | agents(spec-cartographer·spec-writer 신규, integration-qa 재사용) + skills(code-reverse-spec·spec-document-authoring·spec-from-code-orchestrator) | 코드가 기획(00_architecture §56~§65·자동심기·레터박스)을 앞질러 drift 누적 → 코드 역설계 기획문서화 하네스 신규 구축. 영역별 병렬 역설계→통합 합성→정합 검증 파이프라인 |
| 2026-06-17 | 1차 실행 완료 | `_workspace/spec_chunks/{1~6}_*.md`(영역별 조각) + `agenTree-spec.md`(통합 기획문서, 프로젝트 루트) | 6영역 병렬 역설계→통합 합성→QA 교차검증(24항목 계약오류0·행번호오차3 직접교정). 발견 drift: 자동묶기 호출 1곳(activateGrid만, boot/폴 없음)·데이터소스 localStorage 기반·rock 타일 폐기·getHud 죽은 getter·dayIndexFromNewest 항상0. 00_architecture.md 는 히스토리 보존 |
