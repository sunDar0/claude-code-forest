# 🌲 프로젝트 명세서: Claude Code Forest (가제)

## 1. 프로젝트 개요

* **정의:** Claude Code의 실시간 LLM 사용량 메트릭(Telemetry)을 연동하여 무한히 확장되는 대지를 가꾸는 도트 그래픽 풍의 방치형(Idle) 시뮬레이션 게임.
* **핵심 가치:** 개발자의 코딩 생산성 활동이 시각적인 자연 생태계 성장으로 치환되어 심리적 위안과 시각적 카타르시스를 제공함.
* **시점(View):** 시점: 
  * 사선 탑뷰 (Slanted Top-Down / 3/4 View, 45도 앵글)
  * 좌표계: 복잡한 대각선 변환 공식 없이, 일반적인 2차원 배열 격자 grid[x][y]를 그대로 사용.
  * 성장 연출: Input Token이 증가하면 캔버스상에서 나무 기둥의 길이를 위쪽(-Y 방향)으로 늘려줌. Output Token이 증가하면 그 위에 초록색 원을 크게 그려줌.
* **그래픽 방식:** 에셋 파일 없이 오직 코드로만 그래픽을 그려내는 **절차적 생성(Procedural Generation)** 방식.

---

## 2. 핵심 게임 루프 및 시스템 연동

```text
[자원 수집 (LLM 메트릭)] ──> [나무 성장 (Local)] ──> [나무 만렙/독립]
          ▲                                                 │
          └───── [자동 산소로 영구 업그레이드] ◄─── [대지 확장: 1 ➔ 4 ➔ 9]

```

1. **자원 흡수:** Claude Code가 실행될 때마다 발생하는 메트릭을 수집하여 현재 활성화된 타일의 나무에 주입.
2. **나무 독립:** 나무가 최대 레벨에 도달하면 성장을 멈추고 '고목(Matured Tree)' 상태로 전환, 초당 자동 재화(산소) 생산 개시.
3. **대지 확장:** 총 누적 자원 혹은 고목의 수가 마일스톤에 도달하면 대지가 제곱수($N^2$) 형태로 확장되며 새로운 새싹 타일 잠금 해제.

---

## 3. 데이터 구조 및 자원 매핑 시스템

게임 내 모든 오브젝트는 내부 데이터 수치에 비례하여 실시간으로 연산되어 렌더링됩니다.

| LLM 메트릭 | 게임 내 자원 명칭 | 시스템 및 시각적 역할 (데이터 매핑) |
| --- | --- | --- |
| **Input Tokens** | **줄기 양분** | 나무 기둥의 높이(Height)와 높에 맞는 가지를 나타내며  위로 성장시킴. |
| **Output Tokens** | **나뭇잎 에너지** | 기둥 끝에 얹어질 잎사귀 덩어리의 크기와 개수(풍성함)를 결정. |
| **Cache Creation** | **대지 대형 뿌리** | 가끔 발생. 나무의 레벨 제한을 해제하고 기둥의 두께(Width)와 큰 가지를 확장함. |
| **Cache Read** | **황금 수액 (메인 재화)** | 폭발적으로 발생. 나무 줄기 내부를 타고 흐르는 **빛나는 도트 알갱이의 속도/밀도**를 제어, 화폐로 적립됨(사용처 미정). |
| **Request Count** | **생태계 활력** | 질문 횟수. 나무 발치(바닥 타일 영역)에 피어나는 **잔디, 꽃의 밀도 생성. |

---

## 4. 핵심 기술 사양 (Technical Specifications)

### 📐 타일맵 및 그리드 시스템

* **논리적 공간:** 배열 상에서 나무는 1칸($1 \times 1$), 4칸($2 \times 2$), 9칸($3 \times 3$) 점유함 (충돌 연산 최소화).
* **시각적 공간:** 렌더링 시에는 나무의 성장 상태에 따라 주변 타일을 덮어버리는 **오버랩(Overlap)** 허용.
* **그리드 확장 패턴:** 
  * 최초 1개의 그리드(3x3타일)에 어린 묘목(1x1) 생성, 묘목이 자라 유목(2x2), 성목(3x3) 순으로 커진다.
  * 그리드는 최초 그리드 기준으로 8방향에 존재하고, 십자 형태로 그리드를 활성화 시킬수있다.
* **나무는 하루에 하나씩**
  * 나무는 하루에 하나씩만 심을 수 있고, 그날 사용량에 따라 나무의 크기(식생)이 달라진다.
  * 만약 해당일에 클로드를 사용하지 않으면 빈땅, 최대치로 사용하면 성목으로 표현하는식 
### 🎨 절차적 도트 렌더링 규칙

1. **Pixel Perfect:** 화면 렌더링 시 안티앨리어싱(Anti-aliasing)을 강제로 비활성화하여 각진 도트 감성 유지.
2. **Y-Sorting 알고리즘:** 입체감을 위해 모든 오브젝트를 화면 그리기 직전 Y 좌표(세로축) 기준으로 정렬하여 후면 오브젝트부터 순차 렌더링.
```javascript
renderObjects.sort((a, b) => a.y - b.y);

```


3. **수치 스로틀링 (Throttling):** `Cache Read` 수치가 수백만 단위로 폭발하더라도, 화면 내 황금 알갱이의 최대 개수는 무조건 제한(예: Max 40개)하고, 대신 알갱이의 이동 속도와 빛나는 밝기(Glow)로 밀도를 표현하여 시각적 공해를 방지함.

---
## 참조
### Lab('~/study/labs')
* 해당 경로 프로젝트에서 나무 생성 관련 프로젝트를 했던 적이 있음
* 해당 부분 참고 할 수 있는 아키텍처나 방법론이 있는지 참고

---

## 구현 현황 (2026-06-11)

위 §1~§4 는 **초기 구상**이다. 구현을 거치며 아래대로 바뀌었다. 현재 단일 진실 소스는 `_workspace/00_architecture.md`(§1~§37), 변경 이력은 `CLAUDE.md` 표.

### 초기 명세 대비 바뀐 핵심

| 항목 | 초기 명세 | 현재 구현 |
|---|---|---|
| 시점·렌더 | 사선 탑뷰(45도)·labs 이식 | 정사각 타일 탑다운(아이소·labs 폐기, §5)·절차 도트 → 사용자 스프라이트 시트(§30) |
| 단위 | 단일 나무가 1→4→9 타일로 확장 | **하루 = 그리드 1개(3×3 타일)**. 나무는 그리드 안에서 단계로 자람 |
| 확장 | 누적 자원 마일스톤으로 $N^2$ 확장 | 그리드를 8방향 인접(첫 칸은 자유)으로 수동 활성화·하루 하나 |
| 단계 결정 | Input→높이, Output→잎, CacheCreate→두께 | XP/단계(5시간 cap 역산·20/30/50% 경계)로 새싹/어린/고목 |
| 황금 수액 | 줄기 내부 빛 알갱이 흐름 | 성목 주변 금가루 파티클(§13~). 숲 단위는 **캐시 읽기 총량 → 금가루 양**(40캡 스로틀 유지) |
| 계층 | 타일/그리드 | 타일 / 그리드 / 숲(월) / 연간 3계층 + 월간 수동 숲 묶기(§8) |
| 영속 | localStorage 무상태 | 서버 `data/` 영속(일·월·년 파일 분리, 과거 동결, §18) |

### 추가된 규칙 (§37, 2026-06-10~11)

- **그리드 6종**: 활성화 대상 / 활성화됨 / 비활성(흙·잡초·돌) / 활성 불가(숲 바깥 바위 테두리) / 닫힘(그 달에 4방향 둘러싸인 빈칸 = 그 달 땅) / 묶인 숲 바닥.
- **영역 분리**: 달을 묶으면 숲 바깥 4방향은 활성 불가로 잠기고(다음 달이 못 두름), 안쪽 갇힌 빈칸은 닫힘. 차단은 단일 진실 소스(`activation.blockedSet`)로 후보·심기·렌더가 같은 집합을 본다. 인접 후보가 0이면 자유 선택으로 새 숲 시작.
- **숲 살리기**: 묶인 숲 위에 금가루·나비·풀결을 라이브로 덧그려 정지 화면을 살림. 바닥은 얼룩 그늘·톤 변주·잡동사니로 입체화.
- **숲 형태**: 흩어진·사선 배치도 끊김 없이 한 덩어리로 잇되 외곽은 구불구불(직선 금지).