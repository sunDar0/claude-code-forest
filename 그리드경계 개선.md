# 1차 안

## 📐 1. 타일 비트마스킹 & 오토타일링 (Tile Bitmasking & Autotiling)

가장 고전적이고 널리 쓰이는 현업 아키텍처입니다. 잔디 그리드와 흙 그리드가 만날 때, 인접한 8개 칸에 무엇이 있는지 검사하여 경계선 처리용 전용 타일(모서리, 사선 경계 등)을 자동으로 선택해 그리는 기술입니다.

### 작동 원리 (8-Bit Masking)

* 하나의 그리드가 주변 8방향의 인접 격자를 검사합니다.
* 인접한 칸에 같은 종류의 타일(예: 잔디)이 있으면 `1`, 없으면 `0`으로 비트 연산을 수행합니다.
* 이 계산을 통해 `0`부터 `255`까지의 고유 숫자가 나오는데, 이 숫자에 매핑된 경계선 도트 스프라이트를 그려주는 방식입니다. (실제로는 대칭성을 이용해 **47개의 타일**만 있으면 255개의 모든 경계면을 완벽히 커버할 수 있어 이를 **47-Tile Autotiling**이라고 부릅니다.)

---

## 🔀 2. 듀얼 그리드 시스템 (Dual Grid System)

최근 인디 게임 개발 씬에서 가장 뜨거운 찬사를 받은 혁신적인 아키텍처입니다. *타운스케이퍼(Townscaper)*의 개발자 오스카 스탈베르그(Oskar Stålberg)가 정립한 구조로, 기존 비트마스킹의 복잡한 예외 처리(특히 골치 아픈 대각선 코너 처리)를 수학적으로 완전히 파괴한 방식입니다.

### 작동 원리

* **논리적 그리드(Data):** 우리가 만든 $1 \times 1$ 격자(잔디인지 흙인지 담은 데이터)입니다.
* **렌더링 그리드(Display):** 논리적 그리드에서 **정확히 X, Y축으로 반 타일(0.5칸)씩 비틀어서(Offset)** 타일을 그립니다.
* 이렇게 하면 하나의 '렌더링 타일'은 네 개의 '논리적 격자 코너'의 정중앙에 위치하게 됩니다. 네 모서리의 상태(잔디/흙 조합)는 오직 $2^4 = 16$가지 경우의 수밖에 존재하지 않으므로, **단 16개의 전용 경계 타일만으로 전 방향의 자연스러운 블렌딩을 예외 없이 구현**할 수 있습니다. 코딩 난이도가 획기적으로 낮아집니다.

---

## 🔬 3. 학술적 이론 및 관련 논문

그래픽스 학계에서는 이 격자 경계면 결합과 절차적 지형 생성을 수학적으로 해결하기 위해 주로 다음 이론들을 연구해 왔습니다.

### ① 왕 타일 (Wang Tiles) 기반 경계 붕괴 이론

* **핵심 논문:** *Wang Tiles for Image and Texture Generation* (Cohen et al., 2003)
* **내용:** 1961년 수학자 하오 왕(Hao Wang)이 제안한 도미노 이론을 그래픽스에 접목한 것입니다. 타일의 각 변(Edge)에 색상/속성 코드를 부여하고, 인접한 타일끼리 무조건 같은 코드를 가진 변만 맞닿을 수 있도록 타일을 절차적으로 배치하는 알고리즘입니다. 격자 간 텍스처가 끊김 없이 무한히 흐르도록 제어할 때 표준 가이드로 인용됩니다.

### ② 마칭 스퀘어 알고리즘 (Marching Squares)

* **정의:** 2차원 격자 데이터에서 등고선(Boundary)을 추출해내는 알고리즘입니다.
* **응용:** 잔디 데이터가 `1`이고 흙 데이터가 `0`일 때, 경계선 벡터 좌표를 부드러운 곡선 함수(이차 베지에 곡선 등)로 연산해 냅니다. 도트 그래픽보다는 실시간으로 지형이 깎이고 합쳐지는 **웜즈(Worms)** 스타일의 부드러운 경계선 구현에 자주 쓰이는 논문 기반 알고리즘입니다.

---

## 💡 현재 프로젝트(Canvas 2D)에 추천하는 아키텍처

현재 사용자님의 프로젝트는 순수 코드로 구현하는 **절차적 렌더러(Canvas 2D)** 환경입니다.

만약 경계면 처리를 고도화하고 싶으시다면, 이미지 에셋 없이 코드로 원과 사각형을 조합해 그리는 방식이므로 마칭 스퀘어(Marching Squares)의 개념을 도트식으로 단순화한 '절차적 마스킹'을 추천합니다. 인접한 타일이 흙이고 내가 잔디라면, 사각형을 통째로 채우는 게 아니라 경계 방향에 따라 캔버스의 `arc()` 함수나 `quadraticCurveTo()`를 이용해 둥근 모서리 모양으로 파내어 풀밭이 자연스럽게 스며드는 연출을 코드로 쉽게 연산해 낼 수 있습니다.

혹시 지금 숲의 경계선이 사각형으로 너무 딱딱하게 떨어져서 이 결합 로직을 코드에 녹여보고 싶으신 건가요?

# 2차 안

## 💡 해결 아키텍처: 절차적 픽셀 디더링 (Procedural Pixel Dithering)

우리가 앞서 이야기한 오토타일링(Autotiling) 논문들의 핵심 개념을 이미지 파일 없이 캔버스 코드로 구현하는 가장 가볍고 확실한 방법은 "흙 타일이 잔디 타일을 만났을 때, 경계면에 잔디 도트 조각들을 무작위로 침범시켜 그리는 것"입니다.

현재 `renderer.js`에서 땅을 칠하는 방식은 완전한 이분법(`if (this._isActiveTile) continue;`)이라 그렇습니다. 이를 "내가 흙 타일인데, 상하좌우 중 하나라도 풀밭이 있다면 경계면에 풀을 흩뿌린다"는 규칙으로 보완하면 청크 경계선이 완전히 뭉개집니다.

---

## 🛠️ `renderer.js` 코드 수정안

`_drawGroundAndObjects` 메서드 내부의 타일 결 텍스처를 그리는 **②번 루프(`if (!lod)`)** 뒤에 경계면을 부수는 전용 패스(`_drawTileEdges`)를 한 줄만 추가해 주면 됩니다.

### 1. `_drawGroundAndObjects` 내부 수정

```javascript
    // ... 기존 ① 비활성 타일 3색 흙 베이스 루프 종료 후 ...

    // ② 결 텍스처 및 경계면 믹싱 패스
    if (!lod) {
      for (let ty = tyMin; ty <= tyMax; ty++) {
        for (let tx = txMin; tx <= txMax; tx++) {
          const sx = tox + tx * TILE, sy = toy + ty * TILE;
          
          if (this._isActiveTile(tx, ty)) {
            this._drawGrassBlades(b, sx, sy, tx, ty);
          } else {
            this._drawPendingGround(b, sx, sy, tx, ty);
            // 🎯 [추가]: 내가 흙 타일일 때, 주변에 풀밭이 있다면 경계면 도트를 덧그려 뭉갭니다.
            this._blendGridEdge(b, sx, sy, tx, ty);
          }
        }
      }
    }

```

### 2. 새 메서드 `_blendGridEdge` 추가

`renderer.js` 클래스 내부 하단에 아래의 절차적 디더링 함수를 새롭게 주입합니다. 고정 난수(PRNG)를 사용하므로 화면이 깜빡이지 않고 고정됩니다.

```javascript
  // 🎯 잔디-흙 경계면을 픽셀 아틀 스타일로 부수는 절차적 블렌딩 매커니즘
  _blendGridEdge(b, sx, sy, tx, ty) {
    const x = (sx - TILE / 2) | 0;
    const y = (cy - TILE / 2) | 0; // 기존 계산 방식 정합성 유지

    // 상하좌우 이웃 타일이 풀밭(Active)인지 검사
    const L = this._isActiveTile(tx - 1, ty);
    const R = this._isActiveTile(tx + 1, ty);
    const U = this._isActiveTile(tx, ty - 1);
    const D = this._isActiveTile(tx, ty + 1);

    // 주변이 전부 흙이면 경계면이 아니므로 연산 생킵
    if (!L && !R && !U && !D) return;

    // 타일별 고정 난수 생성기
    let seed = ((tx * 73856093) ^ (ty * 19349663)) >>> 0;
    const rnd = () => {
      seed = (seed * 1664525 + 1013904223) >>> 0;
      return seed / 4294967296;
    };

    b.fillStyle = PAL.grassA; // 침범해 들어올 잔디 색상

    // 1) 좌측이 풀밭일 때: 흙 타일의 왼쪽 모서리에 징검다리 도트 배치
    if (L) {
      for (let i = 0; i < TILE; i++) {
        if (rnd() < 0.65) b.fillRect(x, y + i, 1, 1);       // 1px 진입점
        if (rnd() < 0.35) b.fillRect(x + 1, y + i, 1, 1);   // 2px 진입점 (지그재그 효과)
      }
    }
    // 2) 우측이 풀밭일 때: 흙 타일의 오른쪽 모서리
    if (R) {
      for (let i = 0; i < TILE; i++) {
        if (rnd() < 0.65) b.fillRect(x + TILE - 1, y + i, 1, 1);
        if (rnd() < 0.35) b.fillRect(x + TILE - 2, y + i, 1, 1);
      }
    }
    // 3) 상단이 풀밭일 때: 흙 타일의 위쪽 모서리
    if (U) {
      for (let i = 0; i < TILE; i++) {
        if (rnd() < 0.65) b.fillRect(x + i, y, 1, 1);
        if (rnd() < 0.35) b.fillRect(x + i, y + 1, 1, 1);
      }
    }
    // 4) 하단이 풀밭일 때: 흙 타일의 아래쪽 모서리
    if (D) {
      for (let i = 0; i < TILE; i++) {
        if (rnd() < 0.65) b.fillRect(x + i, y + TILE - 1, 1, 1);
        if (rnd() < 0.35) b.fillRect(x + i, y + TILE - 2, 1, 1);
      }
    }

    // [옵션] 경계면에 잔디 결(Speck) 몇 개 더 뿌려주면 투톤이 섞여서 훨씬 자연스러워집니다.
    if (rnd() < 0.4) {
      b.fillStyle = PAL.grassSpeck;
      b.fillRect(x + ((rnd() * TILE) | 0), y + ((rnd() * TILE) | 0), 1, 1);
    }
  }

```
